# SPDX-License-Identifier: curl
# SPDX-FileCopyrightText: the blitzy-curl project contributors
#
# Provenance, traceability, coverage and hazard generator.               DL-0111

[package]
name = "curl-audit-traceability"
description = "Generates the provenance, traceability, coverage and hazard registers"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
publish = false

[lib]
name = "curl_audit_traceability"
path = "src/lib.rs"

[[bin]]
name = "curl-audit-traceability"
path = "src/bin/traceability.rs"

[dependencies]
curl-audit = { path = ".." }
toml = { workspace = true }

[lints]
workspace = true
