<!--
SPDX-FileCopyrightText: the blitzy-curl project contributors
SPDX-License-Identifier: curl
-->

# Quirks and Hazards

Generated by `cargo run --bin curl-audit-traceability`. Run it with
`--check` to compare this file against the tree; a difference, a gap in either
direction, an unresolved locator or a dangling pointer fails the run. Do not
edit this file by hand. Rationale lives in `refactor/docs/DECISION-LOG.md`.

Every hazard the vendored oracle carries that the port has to answer for, with
the verdict the project holds, the crate that owns it and the verification that
proves the verdict. **259 hazard(s)**: **206** reproduced bug for bug, **44**
substituted with an identical observable outcome, **8** diverging and therefore
carrying a divergence-allowlist entry, and **1** belonging to a platform the
matrix excludes. `Status` is `obligation` where the named verification is not
yet written and `verified` only where an artifact exists. Upstream's own record
of known issues is `original/docs/KNOWN_BUGS.md`; this file records what the
port does about the hazards, not a second copy of that record.

## Register

| ID | Locator | Observable behaviour | Verdict | Owner | Compensating verification | Status | Decision |
|---|---|---|---|---|---|---|---|
| HZ-0001 | `original/lib/setup-vms.h:83-101` | The non-void path translator can reach the end of the function after a successful translation, so the value it returns is indeterminate. | not-ported | _none_ | Platform matrix excludes OpenVMS; the construct carries a traceability row and no counterpart. | obligation | DL-0052 |
| HZ-0002 | `original/lib/fake_addrinfo.c:187-199` | An address-conversion allocation failure can return zero with a null result list, reporting a successful resolution that produced nothing. | diverge | `curl-dns` (planned) | Fault-injected conversion failure asserting the deterministic out-of-memory outcome, plus the divergence-allowlist entry. | obligation | DL-0113 |
| HZ-0003 | `original/lib/curl_memrchr.c:39-49` | The fallback search decrements and compares a pointer outside the object it was given. | substitute | `curl-types` (planned) | Caller-shape equivalence test over the reverse search, plus the parser fuzz targets that reach it. | obligation | DL-0048 |
| HZ-0004 | `original/lib/cw-pause.c:179-203` | The pause-buffer trace total accumulates the requested length rather than the number of bytes actually written, so the running total is wrong whenever a short write occurs. | reproduce | `curl-core` (planned) | Differential trace comparison over a transfer paused mid-body. | obligation | DL-0117 |
| HZ-0005 | `original/lib/curl_gssapi.c:238-265` | The oversized display-name path in the debug stub returns without releasing the target-description buffer. | substitute | `curl-auth` (planned) | Leak accounting under the debug allocator feature over an oversized display name. | obligation | DL-0113 |
| HZ-0006 | `original/lib/curl_gssapi.c:225-283` | Several failure paths release the context while leaving the caller's handle pointing at it. | substitute | `curl-auth` (planned) | ABI round-trip under the interpreter over a context step that fails part way. | obligation | DL-0129 |
| HZ-0007 | `original/lib/curl_gssapi.c:312-384` | Provider selection re-reads the stub-credentials environment variable on every call, so the provider can change part way through one context. | substitute | `curl-auth` (planned) | Environment-mutation test between two calls on one context, asserting the provider bound at construction. | obligation | DL-0127 |
| HZ-0008 | `original/lib/curl_share.c:261-328` | Unsharing the connection partition clears the bit without destroying the pool the share object holds. | reproduce | `curl-core` (planned) | Share and unshare matrix over all seven partitions asserting the resulting state and return code. | obligation | DL-0135 |
| HZ-0009 | `original/lib/curl_share.c:35-59` | Unsharing the public-suffix partition answers with the bad-option code although the option is documented as shareable. | reproduce | `curl-core` (planned) | The same matrix, asserting the return code for every partition in both orders. | obligation | DL-0135 |
| HZ-0010 | `original/lib/cshutdn.c:228-263` | The shutdown deadline minimum starts at zero and the comparison selects a smaller value, so no positive time left is ever chosen and the expiry is set only for a negative one. | reproduce | `curl-core` (planned) | Timer-state equivalence test over a connection shutdown that reports a positive time left. | obligation | DL-0120 |
| HZ-0011 | `original/lib/file.c:395-419` | A failed status call leaves the buffer indeterminate, and the directory test later reads it outside the guard that recorded whether the call succeeded. | diverge | `curl-proto-file` (planned) | Fault-injected status-call failure asserting the deterministic not-a-directory outcome, plus the divergence-allowlist entry. | obligation | DL-0113 |
| HZ-0012 | `original/lib/file.c:518-552` | A negative read is treated exactly as end of data, so a read error ends the body and the transfer reports success. | reproduce | `curl-proto-file` (planned) | Equivalence test asserting a read error yields end of data and a successful transfer. | obligation | DL-0113 |
| HZ-0013 | `original/lib/file.c:324-375` | The shared transfer buffer is released after a borrow that failed, over a release path that clears the borrowed flag unconditionally and so can release a borrow another operation holds. | substitute | `curl-proto-file` (planned) | Two-operation borrow test on one multi handle with the first borrow failing. | obligation | DL-0120 |
| HZ-0014 | `original/lib/curl_addrinfo.c:534-600` | The deallocator is chosen by re-reading the resolver environment variable at release time, so a value can be released through a different provider than allocated it. | substitute | `curl-dns` (planned) | Environment-mutation test between allocation and release, asserting the provider stored with the value. | obligation | DL-0127 |
| HZ-0015 | `original/lib/altsvc.c:231-263` | A record-add allocation failure is ignored, so the entry is dropped and the operation reports success. | reproduce | `curl-core` (planned) | Fault-injected allocation failure at the record-add step asserting the reported outcome. | obligation | DL-0113 |
| HZ-0016 | `original/lib/altsvc.c:380-430` | Write and close failures on the save path are ignored, so a short write or a full disk reports success. | reproduce | `curl-core` (planned) | Short-write, disk-full and close-failure fault tests over the save path. | obligation | DL-0124 |
| HZ-0017 | `original/lib/hsts.c:366-423` | A record-add allocation failure is ignored, so the entry is dropped and the operation reports success. | reproduce | `curl-core` (planned) | Fault-injected allocation failure at the record-add step. | obligation | DL-0113 |
| HZ-0018 | `original/lib/hsts.c:557-595` | Write and close failures on the save path are ignored. | reproduce | `curl-core` (planned) | Short-write, disk-full and close-failure fault tests over the save path. | obligation | DL-0124 |
| HZ-0019 | `original/lib/hsts.c:557-595` | A successful write-callback output can mask a save error recorded earlier in the same call. | reproduce | `curl-core` (planned) | Callback-order test with a failing save and a succeeding callback, asserting the reported code. | obligation | DL-0113 |
| HZ-0020 | `original/lib/cookie.c:1500-1601` | Write and close failures on the jar save path can be reported as success. | reproduce | `curl-cookies` (planned) | Short-write and close-failure fault tests over the jar save path. | obligation | DL-0124 |
| HZ-0021 | `original/lib/cookie.c:1060-1065` | Verbose output prints cookie names and values. | reproduce | `curl-cookies` (planned) | Byte comparison of the verbose stream, plus the observability check that no added span field or metric label carries a cookie value. | obligation | DL-0116 |
| HZ-0022 | `original/lib/cookie.c:859-1083` | A replacement releases the record being replaced before the replacement has been allocated, so an allocation failure leaves the jar with neither. | substitute | `curl-cookies` (planned) | Fault-injected allocation failure at the replacement point asserting the previous record survives. | obligation | DL-0124 |
| HZ-0023 | `original/lib/cookie.c:1209-1225` | Callbacks and file operations occur while the application's cookie share lock is held. | reproduce | `curl-cookies` (planned) | Recorded lock-callback sequence over a cookie-enabled transfer compared against the baseline. | obligation | DL-0133 |
| HZ-0024 | `original/lib/curl_ntlm_core.c:299-393` | Backend cipher errors are discarded and the response construction reports success. | reproduce | `curl-auth` (planned) | Known-answer vectors plus a fault-injected backend failure asserting the reported outcome. | obligation | DL-0113 |
| HZ-0025 | `original/lib/curl_ntlm_core.c:410-431` | Password-derived buffers are released without being cleared. | substitute | `curl-auth` (planned) | A test that inspects the derived buffer immediately before release. | obligation | DL-0115 |
| HZ-0026 | `original/lib/getinfo.c:202-217` | Invalid debug numeric input is silently substituted with zero. | reproduce | `curl-core` (planned) | Boundary test over the accepted and rejected inputs asserting the substituted value. | obligation | DL-0117 |
| HZ-0027 | `original/lib/getinfo.c:399-505` | Several size-to-long conversions narrow without a check. | reproduce | `curl-core` (planned) | Boundary test at each narrowing threshold with the marker in place. | obligation | DL-0117 |
| HZ-0028 | `original/lib/asyn-ares.c:257-323` | An HTTPS-record duplication failure loses an entry while the operation continues. | reproduce | `curl-dns` (planned) | Fault-injected duplication failure asserting the resulting record set. | obligation | DL-0113 |
| HZ-0029 | `original/lib/asyn-ares.c:689-706` | A query-name allocation failure returns before the common bookkeeping the other paths perform. | reproduce | `curl-dns` (planned) | Fault-injected allocation failure at the query-name step asserting the bookkeeping state. | obligation | DL-0113 |
| HZ-0030 | `original/lib/asyn-ares.c:257-323` | A null guard is applied to the address of an embedded structure member, which can never be null. | substitute | `curl-dns` (planned) | The guard has no representation in the port; the equivalence test covers the path it guarded. | obligation | DL-0129 |
| HZ-0031 | `original/lib/asyn-ares.c:279-305` | The address conversion records an out-of-memory code in a local and returns only a pointer, so an allocation failure can surface as a successful empty or partial result. | diverge | `curl-dns` (planned) | Torture over the single-family and dual-family conversions, plus the divergence-allowlist entry. | obligation | DL-0113 |
| HZ-0032 | `original/lib/asyn-thrdd.c:211-223` | The HTTPS-record owner name for a port other than 443 is formatted with the underscore after the port rather than before the service label. | reproduce | `curl-dns` (planned) | Query-bytes comparison across providers and ports asserting the emitted owner name. | obligation | DL-0119 |
| HZ-0033 | `original/lib/content_encoding.c:259-347` | Codec close paths do not require end of stream, so a truncated encoded body completes without error. | reproduce | `curl-core` (planned) | Truncated-body corpus case per codec compared against both binaries. | obligation | DL-0118 |
| HZ-0034 | `original/lib/content_encoding.c:730-781` | Identity detection compares only the first eight bytes of the coding name, so a longer name with that prefix is accepted as identity. | reproduce | `curl-core` (planned) | Malformed-coding corpus case over prefix matches. | obligation | DL-0118 |
| HZ-0035 | `original/lib/content_encoding.c:764-781` | The unsolicited-coding path continues silently for a prefix match while the HTTP layer keeps trusting the content length, which permits response desynchronisation on a reused connection. | reproduce | `curl-core` (planned) | Reuse desynchronisation corpus case over two responses on one connection. | obligation | DL-0118 |
| HZ-0036 | `original/lib/dynhds.c:29-49` | The entry allocation sums two caller-supplied lengths and a constant with no overflow check. | substitute | `curl-types` (planned) | Boundary test at the overflow threshold asserting the out-of-memory outcome the caller already handles. | obligation | DL-0114 |
| HZ-0037 | `original/lib/dynhds.c:131-168` | The running-size update is unchecked. | substitute | `curl-types` (planned) | Boundary test over the running size at the overflow threshold. | obligation | DL-0114 |
| HZ-0038 | `original/lib/dynhds.c:113-129` | The case-insensitive lookup returns only the first matching entry, and tunnel authentication uses it as though it were the whole set. | reproduce | `curl-types` (planned) | Differential pair over a response carrying two identically named fields, in both orders. | obligation | DL-0122 |
| HZ-0039 | `original/lib/hash.c:100-118` | The element allocation sums the structure size and the key length with no overflow check. | substitute | `curl-types` (planned) | Boundary test at the overflow threshold asserting the out-of-memory outcome. | obligation | DL-0114 |
| HZ-0040 | `original/lib/easy.c:997-1079` | Duplicate-handle failure cleanup releases a typed cookie jar through a plain free. | substitute | `curl-core` (planned) | Fault-injected allocation failure after the jar has been duplicated, exercised under the interpreter. | obligation | DL-0129 |
| HZ-0041 | `original/lib/doh.c:1083-1132` | A malformed record name can return after the parser buffer has been partly used, without releasing it. | substitute | `curl-dns` (planned) | Malformed and oversized record-name fuzz target. | obligation | DL-0113 |
| HZ-0042 | `original/lib/doh.c:295-447` | A failure to add the probe to the multi handle is ignored and success is returned. | reproduce | `curl-dns` (planned) | Fault-injected multi-add failure asserting the reported code. | obligation | DL-0113 |
| HZ-0043 | `original/lib/doh.c:509-532` | A query-name allocation failure is ignored and success is returned. | reproduce | `curl-dns` (planned) | Fault-injected allocation failure at the query-name step asserting the reported code. | obligation | DL-0113 |
| HZ-0044 | `original/lib/doh.c:1231-1315` | A successful result is added to the cache here and again by the generic front end, so the application's DNS lock callbacks fire twice for one resolution. | reproduce | `curl-dns` (planned) | Recorded lock-callback sequence over one resolution through this path. | obligation | DL-0119 |
| HZ-0045 | `original/lib/doh.c:1139-1192` | A name-decode failure returns out of memory, because that is the initial value of the result variable rather than the cause. | diverge | `curl-dns` (planned) | Decode-failure fuzz corpus asserting the deterministic code, plus the divergence-allowlist entry. | obligation | DL-0119 |
| HZ-0046 | `original/lib/doh.c:1139-1192` | A release build accepts one to three trailing bytes that the debug assertion rejects, so the two build types disagree about the same response. | diverge | `curl-dns` (planned) | Residue fuzz over every trailing length asserting the chosen outcome, plus the divergence-allowlist entry. | obligation | DL-0119 |
| HZ-0047 | `original/lib/cf-h2-proxy.c:179-196` | The peer-shutdown last-stream and error fields are declared and read but never assigned, so the values reported come from initialisation rather than from the frame. | diverge | `curl-http2` (planned) | A proxy shutdown frame with non-zero fields asserting the reported values and an unchanged tunnel outcome, plus the divergence-allowlist and dependency-delta entries. | obligation | DL-0128 |
| HZ-0048 | `original/lib/cf-h2-proxy.c:1018-1032` | Tunnel completion ignores the request soft-reset result. | reproduce | `curl-http2` (planned) | Proxy retry case whose request reader cannot rewind. | obligation | DL-0121 |
| HZ-0049 | `original/lib/cf-socket.c:162-327` | The keepalive unit conversion multiplies an int by a thousand in place with no overflow check. | reproduce | `curl-core` (planned) | Boundary test at the overflow threshold pinning the resulting option value. | obligation | DL-0117 |
| HZ-0050 | `original/lib/cf-socket.c:679-703` | A successful bind to a named interface returns immediately, so a local port the caller also requested is never bound. | reproduce | `curl-core` (planned) | Option-order test requesting an interface and a local port together, and each separately. | obligation | DL-0136 |
| HZ-0051 | `original/lib/cf-socket.c:1422-1441` | The peer-closed text is written into the message buffer and then overwritten by a second error-string call, so the text never reaches the message. | reproduce | `curl-core` (planned) | Byte comparison of the connect-failure message on a connection the peer closed. | obligation | DL-0117 |
| HZ-0052 | `original/lib/cf-socket.c:1625-1629` | A zero-length receive is recorded as first-byte receipt, so the start-transfer timestamp is taken at end of data. | reproduce | `curl-core` (planned) | Comparison of the start-transfer output variable over a connection closed without data. | obligation | DL-0117 |
| HZ-0053 | `original/lib/ftp.c:469-529` | The address of a structure member is taken before the guard that decides whether the structure is usable. | substitute | `curl-proto-ftp` (planned) | The construct has no representation in the port; the equivalence test covers the guarded path. | obligation | DL-0129 |
| HZ-0054 | `original/lib/ftp.c:643-657` | Control-connection errors are discarded on this path. | reproduce | `curl-proto-ftp` (planned) | Fault-injected control-connection failure asserting the reported outcome. | obligation | DL-0113 |
| HZ-0055 | `original/lib/ftp.c:2663-2680` | The response parser reads one byte before the buffer it was given. | substitute | `curl-proto-ftp` (planned) | Index-based parsing plus the FTP response fuzz target over truncated and leading-digit inputs. | obligation | DL-0129 |
| HZ-0056 | `original/lib/ftp.c:2663-2680` | The transfer-type state is entered before the type command has been confirmed. | reproduce | `curl-proto-ftp` (planned) | State-sequence equivalence test over the type negotiation. | obligation | DL-0120 |
| HZ-0057 | `original/lib/ftp.c:3850-3870` | Trace output carries credentials. | reproduce | `curl-proto-ftp` (planned) | Byte comparison of the trace stream, plus the observability check that no added field carries a credential. | obligation | DL-0116 |
| HZ-0058 | `original/lib/ftp.c:3850-3870` | Wildcard callback state is restored incompletely when the connection is reused. | reproduce | `curl-proto-ftp` (planned) | Two-transfer reuse case over one connection with a wildcard pattern on the first transfer only. | obligation | DL-0130 |
| HZ-0059 | `original/lib/http.c:1080-1112` | The challenge scan advances on any comma, so a comma inside a quoted parameter value splits one challenge into two. | reproduce | `curl-http1` (planned) | Quoted-comma challenge corpus in single-field and multi-field forms asserting the selected mechanism. | obligation | DL-0131 |
| HZ-0060 | `original/lib/http.c:1657-1661` | The final header-write result is discarded, so a write-callback error on the last header block is dropped. | reproduce | `curl-http1` (planned) | Failing write callback on the last header block asserting the reported exit. | obligation | DL-0113 |
| HZ-0061 | `original/lib/http.c:2624-2657` | A zero or unknown upload length produces a content-range header no specification permits. | reproduce | `curl-http1` (planned) | Request-bytes comparison over zero-length and unknown-length upload resume. | obligation | DL-0132 |
| HZ-0062 | `original/lib/http.c:2551-2584` | Cookie work, including a reachable write callback, runs while the application's cookie lock is held. | reproduce | `curl-http1` (planned) | Recorded lock-callback sequence over a cookie-enabled transfer. | obligation | DL-0133 |
| HZ-0063 | `original/lib/http.c:4265-4313` | The status line is read at a fixed offset rather than parsed forward. | reproduce | `curl-http1` (planned) | Status-line corpus with unusual but accepted shapes asserting the parsed status. | obligation | DL-0131 |
| HZ-0064 | `original/lib/http.c:4265-4313` | Trace output carries credentials and challenge values. | reproduce | `curl-http1` (planned) | Byte comparison of the trace stream, plus the no-new-sink observability check. | obligation | DL-0116 |
| HZ-0065 | `original/lib/http.c:3599-3616` | An invalid identity-prefix coding leaves the content length trusted. | reproduce | `curl-http1` (planned) | Malformed-coding and content-length corpus compared against both binaries. | obligation | DL-0118 |
| HZ-0066 | `original/lib/http.c:3788-3811` | The response path can leave queued bytes on a connection that is then reused. | reproduce | `curl-http1` (planned) | Reuse desynchronisation corpus over two responses on one connection. | obligation | DL-0118 |
| HZ-0067 | `original/lib/http2.c:699-708` | A stream setup allocation failure is ignored. | reproduce | `curl-http2` (planned) | Fault-injected stream setup failure asserting the reported outcome. | obligation | DL-0113 |
| HZ-0068 | `original/lib/http2.c:777-886` | The promise-header store is released on a failure path while callbacks may still be pending against it. | substitute | `curl-http2` (planned) | Callback-lifetime test failing a stream with a promise pending, under the interpreter. | obligation | DL-0134 |
| HZ-0069 | `original/lib/http2.c:967-1007` | A recorded stream error can be overwritten by a later one, so the first cause is lost. | reproduce | `curl-http2` (planned) | Two-error sequence on one stream asserting the reported code. | obligation | DL-0134 |
| HZ-0070 | `original/lib/http2.c:2429-2452` | An interim informational response completes the header phase. | reproduce | `curl-http2` (planned) | Response sequence carrying one and then several informational responses before the final one. | obligation | DL-0134 |
| HZ-0071 | `original/lib/http2.c:2429-2452` | An upgrade-binding failure is ignored. | reproduce | `curl-http2` (planned) | Fault-injected binding failure on the upgrade path asserting the reported outcome. | obligation | DL-0113 |
| HZ-0072 | `original/lib/http2.c:2429-2452` | Trace output carries header values. | reproduce | `curl-http2` (planned) | Byte comparison of the trace stream, plus the no-new-sink observability check. | obligation | DL-0116 |
| HZ-0073 | `original/lib/http2.c:1422-1485` | The promise store caps the number of header pointers it holds but places no limit on their total size. | diverge | `curl-http2` (planned) | Promises with very many small fields and with very few very large fields, asserting the limit and the resulting stream error, plus the divergence-allowlist and dependency-delta entries. | obligation | DL-0125 |
| HZ-0074 | `original/lib/http_aws_sigv4.c:323-368` | A duplicate-header merge leaks the merged value when the append fails. | substitute | `curl-auth` (planned) | Fault-injected append failure with leak accounting. | obligation | DL-0113 |
| HZ-0075 | `original/lib/http_aws_sigv4.c:1095-1162` | The signing secret and the intermediate hashes are released without being cleared. | substitute | `curl-auth` (planned) | Known-answer vectors plus a test that inspects the intermediates immediately before release. | obligation | DL-0115 |
| HZ-0076 | `original/lib/headers.c:221-244` | The internal push path indexes byte zero and assumes a non-empty input. | substitute | `curl-core` (planned) | The precondition becomes a type the caller cannot violate; the parser boundary test covers empty and blank inputs. | obligation | DL-0123 |
| HZ-0077 | `original/lib/headers.c:121-137` | The iterating public entry point dereferences its handle unchecked, unlike the indexed one beside it. | reproduce | `curl-abi` (planned) | C-side invalid-handle test in a subprocess, executed on both Linux targets. | obligation | DL-0123 |
| HZ-0078 | `original/lib/hostip.c:781-924` | The synchronous timeout uses a process-global alarm signal and a non-local jump, with connection-lifetime assumptions that depend on re-entry on the same stack. | reproduce | `curl-dns` (planned) | Configuration-scoped equivalence test that runs only where the guard holds, plus an assertion that the default configuration reaches no signal path. | obligation | DL-0126 |
| HZ-0079 | `original/lib/hostip.c:430-465` | Asynchronous backend errors collapse into host or proxy resolve errors, losing the backend's own cause. | reproduce | `curl-dns` (planned) | Error-taxonomy test per backend failure class asserting the reported code. | obligation | DL-0119 |
| HZ-0080 | `original/lib/hostip.c:1037-1077` | A collapsed error is negatively cached. | reproduce | `curl-dns` (planned) | Negative-cache duration test asserting the code the next call reports. | obligation | DL-0119 |
| HZ-0081 | `original/lib/hostip.c:1061-1067` | The generic front end performs a second cache insertion for a result the resolver path already added. | reproduce | `curl-dns` (planned) | Recorded lock-callback sequence over one resolution asserting the insertion count. | obligation | DL-0119 |
| HZ-0082 | `original/lib/curl_sasl.c:846-908` | The digest and NTLM built-in booleans are inverted against their siblings, so the explanation line is selected on the opposite condition. | reproduce | `curl-auth` (planned) | Verbose-output comparison in each feature configuration. | obligation | DL-0117 |
| HZ-0083 | `original/lib/cf-h1-proxy.c:751-864` | The authentication retry and the tunnel completion ignore the request soft-reset and rewind result. | reproduce | `curl-http1` (planned) | Proxy retry case whose request reader cannot rewind. | obligation | DL-0121 |
| HZ-0084 | `original/lib/cf-h1-proxy.c:326-353` | Numeric content-length prefixes are accepted without full validation. | reproduce | `curl-http1` (planned) | Tunnel framing corpus over numeric content-length prefixes. | obligation | DL-0118 |
| HZ-0085 | `original/lib/cf-h1-proxy.c:413-525` | Conflicting content-length fields and unknown transfer codings are incompletely validated. | reproduce | `curl-http1` (planned) | Tunnel framing corpus over conflicting lengths and unknown codings. | obligation | DL-0118 |
| HZ-0086 | `original/lib/cf-h1-proxy.c:751-772` | A proxy connection can be reused with unread response bytes after an authentication retry. | reproduce | `curl-http1` (planned) | Authentication-retry and reconnect matrix over one proxy connection. | obligation | DL-0118 |
| HZ-0087 | `original/lib/http_proxy.c:464-507` | Tunnel authentication examines only the first matching authenticate field. | reproduce | `curl-http1` (planned) | Differential pair over duplicate authenticate fields in both orders. | obligation | DL-0122 |
| HZ-0088 | `original/lib/curl_fopen.c:99-153` | The final path is opened and truncated, and a symlink followed, before the protected temporary file is created. | reproduce | `curl-types` (planned) | Filesystem matrix over a regular file, a symlink outside the directory, a named pipe, a device node, an unwritable directory and a truncated write. | obligation | DL-0124 |
| HZ-0089 | `original/lib/formdata.c:383-388` | A negative name length becomes a very large unsigned size. | reproduce | `curl-core` (planned) | C-side variadic argument-class test over the negative length. | obligation | DL-0123 |
| HZ-0090 | `original/lib/formdata.c:553-560` | The filename and buffer options measure a pointer the caller may pass as null. | reproduce | `curl-core` (planned) | C-side variadic argument-class test in a subprocess. | obligation | DL-0123 |
| HZ-0091 | `original/lib/cf-haproxy.c:84-97` | The client-address override is interpolated verbatim into a line-oriented preamble, so a carriage return or line feed injects a field. | reproduce | `curl-core` (planned) | Request-bytes comparison over control-bearing values. | obligation | DL-0132 |
| HZ-0092 | `original/lib/cf-haproxy.c:84-97` | Every override value that is not a dotted-quad literal is labelled as the IPv6 family. | reproduce | `curl-core` (planned) | Request-bytes comparison over non-address values. | obligation | DL-0060 |
| HZ-0093 | `original/lib/curl_sspi.c:109-176` | Plaintext password copies are released without being cleared. | substitute | `curl-auth` (planned) | A test that inspects the copy immediately before release. | obligation | DL-0115 |
| HZ-0094 | `original/lib/curl_sspi.c:198-204` | The release path frees the plaintext copies without clearing them. | substitute | `curl-auth` (planned) | The same test over the release path. | obligation | DL-0115 |
| HZ-0095 | `original/lib/hmac.c:45-123` | Key-derived hash states are released without being cleared. | substitute | `curl-auth` (planned) | Known-answer vectors plus a test that inspects the state immediately before release. | obligation | DL-0115 |
| HZ-0096 | `original/lib/uint-spbset.c:223-253` | Ordered iteration increments the caller's value before it searches, so a set holding UINT32_MAX keeps answering UINT32_MAX and the walk never terminates, and a call carrying the exhaustion sentinel back in restarts at the smallest member instead of reporting the set exhausted. | reproduce | `curl-types` (planned) | Rust-native equivalence test over ordered iteration with UINT32_MAX stored and with the exhaustion sentinel passed back in, asserting the oracle's own measured answers under a bounded guard. | obligation | DL-0145 |
| HZ-0097 | `original/lib/tftp.c:347-383` | The block number wraps at an unsigned 16-bit boundary, and the transmit path additionally accepts an acknowledgement for 65535 while it is expecting block 0, a workaround for a third-party server that the receive path does not carry. | reproduce | `curl-proto-tftp` (planned) | Differential transfer crossing the 65535-block boundary, asserting the acknowledgement is accepted rather than retried, plus a receive-side test asserting an unexpected data block is logged and retried rather than accepted. | obligation | DL-0147 |
| HZ-0098 | `original/lib/transfer.c:258-267` | The rate-limited receive comment describes breaking out once the loop already has data and less than 16k of allowance is left, but the code breaks only when the allowance is exhausted and otherwise clamps the read length to it, so the described stutter threshold does not exist. | reproduce | `curl-core` (planned) | Differential comparison of read lengths, write-callback counts and elapsed pacing over a --limit-rate download. | obligation | DL-0148 |
| HZ-0099 | `original/lib/transfer.c:228-313` | The receive loop counter is initialised to ten and tested by post-decrement in a do-while, so the body runs eleven times per invocation rather than ten before the loop yields. | reproduce | `curl-core` (planned) | Counting receive attempts and write-callback batches per curl_multi_socket_action call against the oracle. | obligation | DL-0148 |
| HZ-0100 | `original/lib/mqtt.c:605-627` | The remaining-length decoder rejects a fifth continuation byte and a field whose last byte still carries the continuation bit, and rejects an empty field through that same predicate; the decoded value is bounded only by the four-byte cap. | reproduce | `curl-proto-mqtt` (planned) | Varint boundary test over the one to four byte encodings, the five-byte encoding, a field truncated after each byte and an empty field, asserting the reject decision and the decoded value. | obligation | DL-0150 |
| HZ-0101 | `original/lib/mqtt.c:876-893` | An end of stream part way through the remaining-length field leaves a partial field that the decoder then rejects, so a truncated fixed header is reported as a weird server reply rather than as a receive or partial-file error. | reproduce | `curl-proto-mqtt` (planned) | Broker-side truncation test cutting the connection after the first byte and after each remaining-length byte, asserting the reported code against both trees. | obligation | DL-0150 |
| HZ-0102 | `original/lib/mqtt.c:172-186` | The remaining-length encoder emits no byte at all for a zero length and returns a zero count, and it silently drops the high bits of a value needing a fifth byte; the callers bound the value instead, and the publish path bounds it only after the encoder has run. | reproduce | `curl-proto-mqtt` (planned) | Direct encoder test over zero, each byte-count boundary and a value past the four-byte cap, since no caller reaches the zero case. | obligation | DL-0150 |
| HZ-0103 | `original/lib/mqtt.c:296-344` | Four faults of the client's own making are reported as a weird server reply: an over-large connect packet, a client-identifier length mismatch, an over-long username and an over-long password. | reproduce | `curl-proto-mqtt` (planned) | Error-code table test driving each of the four faults and asserting the code, the message and the exit status against both binaries. | obligation | DL-0151 |
| HZ-0104 | `original/lib/mqtt.c:308-316` | The random draw that fills the client identifier has its result stored and then not consulted, so a failed draw is overwritten and reported as a client-identifier length mismatch instead of as the random-source failure. | reproduce | `curl-proto-mqtt` (planned) | Fault-injected random-source failure asserting the reported code and message, plus a connect-bytes comparison under the deterministic entropy override. | obligation | DL-0151 |
| HZ-0105 | `original/lib/mqtt.c:247-264` | The connect packet always advertises a sixty-second keep-alive although the comment above the two bytes says the value disables it, and no option changes it; ping requests are driven by the upkeep interval instead. | reproduce | `curl-proto-mqtt` (planned) | Byte comparison of the emitted connect packet across both trees, and a ping-cadence test driven by the upkeep interval. | obligation | DL-0151 |
| HZ-0106 | `original/lib/mqtt.c:401-433` | A connect acknowledgement whose remaining length is not two is rejected before its bytes are read, and any non-zero broker return code then collapses to a weird server reply with the two bytes printed and no reason mapped. | reproduce | `curl-proto-mqtt` (planned) | Broker-side test returning each defined acknowledgement return code and a wrong remaining length, asserting one code and the printed bytes. | obligation | DL-0151 |
| HZ-0107 | `original/lib/mqtt.c:505-541` | A subscribe acknowledgement whose remaining length is not three is rejected with a message, while a packet-identifier or return-code mismatch is rejected with the same code and no message at all. | reproduce | `curl-proto-mqtt` (planned) | Broker-side test returning a mismatched packet identifier, a failure return code and a wrong remaining length, asserting the code and the presence or absence of the message. | obligation | DL-0151 |
| HZ-0108 | `original/lib/mqtt.c:435-451` | The topic is URL-decoded with the do-nothing reject setting and bounded only by length, so a percent-encoded zero byte or control byte reaches the wire verbatim while the topic is also handed to a C string length call. | reproduce | `curl-proto-mqtt` (planned) | Subscribe and publish byte comparison over topics carrying a percent-encoded zero byte, control bytes and non-ASCII bytes, plus the over-long topic boundary. | obligation | DL-0150 |
| HZ-0109 | `original/lib/mqtt.c:545-567` | Publishing takes its payload only from the post-fields option and has no read-callback path, and a missing payload is reported with a bad-argument code whose only message is inside a debug-build macro. | reproduce | `curl-proto-mqtt` (planned) | Publish test with the payload absent and with an upload requested through the read callback, asserting the code and the message in both a debug and a release configuration. | obligation | DL-0151 |
| HZ-0110 | `original/lib/mqtt.c:709-753` | Each published message resets the download size, the byte counter and the request size, and the raw packet remainder is written to the client, so a subscription delivers the two-byte topic length, then the topic, then the payload for every message rather than payload bytes only. | reproduce | `curl-proto-mqtt` (planned) | Multi-message subscription test comparing the delivered bytes, the progress meter and the download-size output variable against both binaries. | obligation | DL-0151 |
| HZ-0111 | `original/lib/mqtt.c:729-734` | A read that would block inside a published message emits the literal verbose line EEEE AAAAGAIN. | reproduce | `curl-proto-mqtt` (planned) | Verbose byte comparison over a subscription whose payload arrives split so the read blocks part way through. | obligation | DL-0151 |
| HZ-0112 | `original/lib/mqtt.c:1007-1028` | The protocol descriptor fills the doing slots and no performing-state slot, so the whole transfer runs in the doing state and never reaches the state that applies the transfer-rate limit. | reproduce | `curl-proto-mqtt` (planned) | Rate-limited subscription test asserting the limit has no effect, compared against both binaries. | obligation | DL-0151 |
| HZ-0113 | `original/lib/mqtt.c:143-148` | Every outgoing packet is written to the trace stream as header data, so the connect packet's username and password and the published payload appear there in plaintext. | reproduce | `curl-proto-mqtt` (planned) | Byte comparison of the trace stream over an authenticated publish, plus the observability check that no added span field or metric label carries a credential or a payload. | obligation | DL-0151 |
| HZ-0114 | `original/lib/socks.c:937-1016` | The SOCKS5 reply read demands eight bytes before parsing while a domain-name reply carrying a zero-length name is seven, so the handshake consumes one byte of the stream that follows and drops it with the filter's buffer. | reproduce | `curl-core` (planned) | Stub-proxy differential case replying with address type 3, a zero-length name and trailing application bytes, asserting both trees lose the same byte. | obligation | DL-0152 |
| HZ-0115 | `original/lib/socks.c:1075-1084` | The GSS-API leg is entered from a non-blocking connect filter and described as non-blocking, while the negotiation it calls blocks on socket reads, bounded by the transfer deadline where one is set and unbounded where none is. | reproduce | `curl-core` (planned) | Socket-callback and timeout equivalence test over a SOCKS5 GSS-API handshake, asserting the event loop is not re-entered mid-context. | obligation | DL-0152 |
| HZ-0116 | `original/lib/socks.c:678-728` | A proxy selecting username and password authentication it was never offered, with no credentials configured, is answered with a sub-negotiation frame carrying a zero-length username and a zero-length password, which the one-to-255 fields of that sub-negotiation do not allow. | reproduce | `curl-core` (planned) | Byte comparison over a stub proxy that selects the username and password method unsolicited with no credentials set. | obligation | DL-0152 |
| HZ-0117 | `original/lib/strequal.c:51-64` | The length-bounded case-insensitive compare treats its bound as a ceiling rather than a byte count: it stops at the first NUL of the left argument, so two buffers that differ only after an embedded NUL answer equal at a bound that spans the difference. | reproduce | `curl-types` (planned) | Rust-native equivalence test over embedded-NUL inputs at bounds below, at and beyond the terminator, plus the ABI round-trip through the exported pair. | obligation | DL-0154 |
| HZ-0118 | `original/lib/strequal.c:86-95` | The zero-length answer turns on pointer nullness: a non-null pair answers equal at a bound of zero while two null pointers answer not equal at that same bound and equal at every non-zero bound. | reproduce | `curl-types` (planned) | Rust-native equivalence test over the null and zero-bound combinations, plus the ABI round-trip asserting the returned code for each. | obligation | DL-0154 |
| HZ-0119 | `original/lib/socks_sspi.c:460-465` | The negotiated protection level is recorded on the connection and then never applied, so no subsequent proxy message is wrapped and the trace says so. | reproduce | `curl-auth` (planned) | A test that drives the subnegotiation to a granted protection level and asserts the bytes that follow are unwrapped. | obligation | DL-0155 |
| HZ-0120 | `original/lib/socks_sspi.c:140-529` | The socket is forced blocking for the whole subnegotiation and read back with blocking reads, so the calling thread blocks even under the multi interface. | reproduce | `curl-auth` (planned) | A test that asserts the subnegotiation blocks and that non-blocking mode is restored on both the success and the failure exit. | obligation | DL-0155 |
| HZ-0121 | `original/lib/socketpair.c:38-47` | The eventfd implementation assigns one descriptor to both halves of the pair, so the read half and the write half are the same descriptor and the counter it carries collapses any number of signals into one readable event. | reproduce | `curl-core` (planned) | Wakeup-channel equivalence test asserting one readable event after repeated signals and a single close at teardown on the eventfd rung. | obligation | DL-0157 |
| HZ-0122 | `original/lib/socketpair.c:55-127` | The pipe and socketpair implementations return failure from their creating call without writing the pair, leaving the caller's two descriptors at whatever value they already held, while every other failure path in the unit sets both to the invalid value. | substitute | `curl-core` (planned) | Fault-injected creation failure asserting an invalid pair on return, plus the multi-handle creation-failure test that reaches it. | obligation | DL-0157 |
| HZ-0123 | `original/lib/socketpair.c:215-243` | The loopback fallback discards the result of the handshake write, so a write that sends nothing leaves the verification read polling until the sixty-second deadline before the pair is reported as failed. | reproduce | `curl-core` (planned) | Loopback-fallback handshake test built on the rung with no eventfd, pipe or socketpair, asserting the deadline-bounded failure; the rung is selected on no platform in the matrix, so the result is recorded as deferred. | obligation | DL-0157 |
| HZ-0124 | `original/lib/rand.c:106-130` | The debug entropy override seeds a function-local static counter once per process and advances it on every later draw with no synchronisation, so two handles drawing from two threads race on it and the value a transfer sees follows from how many draws preceded it. | substitute | `curl-types` (planned) | Two-thread draw test under the debug feature asserting one distinct value per draw, plus the ten upstream cases that set the variable. | obligation | DL-0160 |
| HZ-0125 | `original/lib/rand.c:77-97` | The fallback draw seeds a static from the wall clock and advances it with a linear congruential step, so a build carrying neither a TLS generator nor arc4random produces predictable boundaries, nonces and masking keys from unsynchronised shared statics. | substitute | `curl-types` (planned) | No-TLS configuration test asserting the platform generator backs the draw and the weak-seed warning line is byte-unchanged. | obligation | DL-0158 |
| HZ-0126 | `original/src/config2setopts.c:1042-1091` | A benign option outcome the unit itself tolerates, being not-built-in or unknown-option left in the result variable by the string-option macro, survives every later option call because none of them assigns that variable, and is returned to the caller, which fails the whole transfer on any non-zero value. | reproduce | `curl-cli` (planned) | Differential exit-code and stderr comparison over a configuration whose capability is absent, asserting both trees abandon the transfer with the same code rather than continuing. | obligation | DL-0162 |
| HZ-0127 | `original/src/config2setopts.c:712-889` | The progress switch is set from the global quiet flags and then overridden by the callback group invoked immediately afterwards, so a standard-input upload re-enables progress reporting only because the later call wins; the source comment states the dependency and nothing enforces it. | reproduce | `curl-cli` (planned) | Differential comparison of a standard-input upload over both trees, asserting the read-busy callback is reached and that stdout, stderr and the emitted option list match byte for byte. | obligation | DL-0162 |
| HZ-0128 | `original/src/config2setopts.c:74-98` | The type-of-service switch has arms for IPv4 and IPv6 only and leaves its result at zero for any other address family, so a requested type of service is silently not applied and no warning is printed; the IPv4 arm is likewise empty on a platform without the IPv4 option macro. | reproduce | `curl-cli` (planned) | Differential stderr and applied-socket-option comparison for a requested type of service on a unix-socket transfer, asserting neither tree warns and neither applies the option. | obligation | DL-0162 |
| HZ-0129 | `original/src/config2setopts.c:340-352` | The embedded-CA note for proxies is printed on every verbose transfer whose proxy CA options are unset, including transfers that configure no proxy at all and transfers that supplied their own CA file, and the blob is installed on the handle in those cases too. | reproduce | `curl-cli` (planned) | Differential stderr comparison of a verbose secure transfer with and without an explicit CA file, asserting two notes in the first case and only the proxy note in the second. | obligation | DL-0162 |
| HZ-0130 | `original/src/config2setopts.c:838-847` | In a debug build an unparsable buffer-size environment value makes the helper return without setting the buffer size at all, so the transfer runs on the library default instead of the tool's own hundred-kilobyte value, and nothing is reported. | reproduce | `curl-cli` (planned) | Debug-feature test setting that environment value to a non-number and asserting the emitted option list carries no buffer-size entry in either tree. | obligation | DL-0162 |
| HZ-0131 | `original/src/config2setopts.c:251-296` | An unset minimum TLS version becomes 1.2 unless the requested maximum is lower, in which case the minimum is lowered to that maximum, and any version value above the highest known one falls through to 1.3 rather than being rejected. | reproduce | `curl-cli` (planned) | Differential comparison of the emitted TLS version option across every minimum and maximum version combination the tool accepts, including values above the highest known one. | obligation | DL-0162 |
| HZ-0132 | `original/lib/strcase.c:72-84` | The ASCII case converters return a char of implementation-defined signedness, and two callers widen that return into an unsigned integer used as a hash, so for any byte at or above 0x80 the bucket selected differs between the signed-char and unsigned-char targets of the platform matrix. | substitute | `curl-types` (planned) | Converted-value equivalence over all 256 inputs, plus a differential cookie-jar and request-header comparison over a non-ASCII cookie domain and a scheme-lookup comparison over a non-ASCII scheme, run on both execution-verified platforms. | obligation | DL-0164 |
| HZ-0133 | `original/lib/md5.c:26-27` | MD5 is compiled in and reachable whenever Digest authentication or the built-in NTLM core is, gated on nothing else, so a transfer puts MD5-derived authentication material on the wire by default with no strength check, no opt-out and no warning. | reproduce | `curl-auth` (planned) | The upstream MD5 unit case with the Digest and CRAM-MD5 protocol cases against both trees, plus known-answer vectors over the one-shot and staged interfaces asserting one digest across every feature configuration. | obligation | DL-0165 |
| HZ-0134 | `original/lib/md5.c:602-610` | Finalising releases both the hash context and the context structure while returning success, so the caller holds a freed pointer and a second finalise or update on the same context reads freed memory. | substitute | `curl-auth` (planned) | Leak accounting under the debug allocator over init, update and finalise, plus a single-use equivalence test asserting the context cannot be reused after finalising. | obligation | DL-0165 |
| HZ-0135 | `original/lib/netrc.c:272-308` | A carriage return run that is not followed by a newline leaves the lexer positioned on a blank, so the token that follows measures zero bytes: in keyword position that is a syntax error which fails the transfer, while in value position the very next statement overwrites the error and the value becomes the empty string. | reproduce | `curl-auth` (planned) | The netrc parser fuzz target plus a differential pair over the same bytes in both positions, asserting the syntax error and the empty value. | obligation | DL-0167 |
| HZ-0136 | `original/lib/netrc.c:76-85` | A file, a line or a token over its size cap makes the dynamic buffer answer too-large, which the error translation folds into a syntax error, so an oversized but grammatical netrc is reported as malformed and is tolerated when the netrc level is optional. | reproduce | `curl-auth` (planned) | Differential cases at each of the three caps against both netrc levels, asserting the syntax-error code rather than an allocation failure. | obligation | DL-0167 |
| HZ-0137 | `original/lib/netrc.c:521-524` | Comment removal happens while the file is loaded, before any token is read, so a line whose first non-blank byte is a hash is deleted from inside a multi-line quoted value and no netrc can express such a line. | reproduce | `curl-auth` (planned) | Differential pair over a quoted value spanning a hash line and the same value without it, asserting the shortened value. | obligation | DL-0167 |
| HZ-0138 | `original/lib/proxy.c:236-269` | Only the first entry of a blank-separated no-proxy list is compared: the token scan stops at a space or a tab, and the loop then ends on any following character that is not a comma, so every later entry is never tested. | reproduce | `curl-core` (planned) | Rust-native replacement of upstream case 1614 carrying its blank-separated row, plus the --noproxy invocation of the one-per-option differential corpus. | obligation | DL-0171 |
| HZ-0139 | `original/lib/proxy.c:217-218` | The asterisk override is an exact equality against the whole value, so an asterisk beside other entries, or one padded with blanks, is treated as a host pattern and matches nothing. | reproduce | `curl-core` (planned) | Rust-native replacement of upstream case 1614 extended with the asterisk-in-a-list and blank-padded-asterisk rows. | obligation | DL-0171 |
| HZ-0140 | `original/lib/proxy.c:72-86` | An IPv4 prefix length of zero is not a match-everything: the masked comparison is skipped for zero and for thirty-two and the code falls through to an exact address equality, so 0.0.0.0/0 matches only 0.0.0.0. | reproduce | `curl-core` (planned) | Rust-native replacement of upstream case 1614, whose IPv4 table carries the zero-bit rows for an equal and an unequal address, plus a zero-prefix entry in the --noproxy corpus. | obligation | DL-0171 |
| HZ-0141 | `original/lib/proxy.c:73-82` | The masked IPv4 comparison converts both network-order addresses with the host-to-network helper and not with its inverse, which is the same byte swap on every platform in the matrix. | substitute | `curl-core` (planned) | Rust-native replacement of upstream case 1614, asserting the same accept and reject decisions from big-endian reads of both addresses. | obligation | DL-0171 |
| HZ-0142 | `original/lib/proxy.c:101-107` | An IPv6 prefix length of zero is rewritten to a full-length prefix, and a length covering more than the address is rejected before either address is parsed, so ::/0 matches only :: and an over-long prefix is indistinguishable from an unparsable address. | reproduce | `curl-core` (planned) | Rust-native replacement of upstream case 1614, whose IPv6 table carries the zero, full-length and over-length prefix rows. | obligation | DL-0171 |
| HZ-0143 | `original/lib/proxy.c:167-173` | A no-proxy entry of 128 bytes or more can never match an address host: the cap is on the whole token and not on the address inside it, and the entry is rejected before any parse is attempted. | reproduce | `curl-core` (planned) | Rust-native replacement of upstream case 1614, whose table carries the 127-byte and 128-byte entry rows. | obligation | DL-0171 |
| HZ-0144 | `original/lib/proxy.c:175-189` | Everything before the first slash of an address entry must parse as a bare address, so a port-qualified entry and a bracketed IPv6 entry never match; the prefix length is bounded at 128 by the parse and again by the address family, and leading zeroes in it are accepted. | reproduce | `curl-core` (planned) | Rust-native replacement of upstream case 1614 extended with the port-qualified, bracketed-literal and out-of-range prefix rows for both families. | obligation | DL-0171 |
| HZ-0145 | `original/lib/proxy.c:137-157` | The trailing-dot trim reads the entry's last byte before the entry is tested for emptiness, and an entry of one dot becomes empty and then tail-matches through a zero-length comparison, so it matches a host ending in two dots and the host that is a single dot. | reproduce | `curl-core` (planned) | Rust-native replacement of upstream case 1614 extended with the single-dot entry rows, and the URL corpus rows whose host ends in a dot. | obligation | DL-0171 |
| HZ-0146 | `original/lib/proxy.c:152-156` | Host comparison folds only the twenty-six ASCII letters, so bytes above the ASCII range compare literally and two spellings differing only by a non-ASCII case pair do not match. | reproduce | `curl-core` (planned) | Rust-native replacement of upstream case 1614 extended with a non-ASCII case-pair row, asserted over bytes with no Unicode case API on the path. | obligation | DL-0171 |
| HZ-0147 | `original/lib/smtp.c:374-375` | The dot-escaping reader adjusts the configured input size, and only when that size is already positive, after the MAIL FROM size parameter at :1031 and the progress upload total at :1475 have both read it, so the announced and reported sizes exclude every stuffed dot and the terminating sequence. | reproduce | `curl-proto-smtp` (planned) | Upstream tests 912 and 913 fix the announced size bytes; a Rust-native test asserts the announced value for a body whose first line begins with a dot. | obligation | DL-0178 |
| HZ-0148 | `original/lib/smtp.c:402-415` | The end-of-body trace line reports CRLF-dot-CRLF unconditionally, while the bytes appended are dot-CRLF for a body already ending in CRLF and dot-CRLF-dot-CRLF for one ending in CRLF-dot. | reproduce | `curl-proto-smtp` (planned) | Trace output compared byte for byte against the oracle with the SMTP trace feature enabled, over all three body endings. | obligation | DL-0178 |
| HZ-0149 | `original/lib/smtp.c:1784-1787` | The per-transfer fields eob at :132 and trailing_crlf at :136 are written here and are then never read anywhere in the unit, the reader context counter n_eob at :304 being what actually tracks the end-of-body match. | substitute | `curl-proto-smtp` (planned) | The field-propagation audit carries both as reserved with a write-site and no read-site, and the port omits them; upstream SMTP upload cases pass unchanged. | obligation | DL-0178 |
| HZ-0150 | `original/lib/smtp.c:305-306` | The reader context field eob is declared and is never written or read, the counter actually used being n_eob at :304. | substitute | `curl-proto-smtp` (planned) | The field-propagation audit carries it as reserved with neither a write-site nor a read-site, and the port declares only the counter; the dot-escaping cases pass unchanged. | obligation | DL-0178 |
| HZ-0151 | `original/lib/smtp.c:552-559` | The reply-message extractor relies on unsigned wraparound: the trailing-blank scan leaves the length at the maximum of its type once every trailing byte is blank or newline, so the pre-incremented store addresses offset zero and the extracted message is zero length. | reproduce | `curl-proto-smtp` (planned) | A Rust-native test drives the extractor with an all-blank remainder and asserts a zero-length message rather than a panic. | obligation | DL-0178 |
| HZ-0152 | `original/lib/rtsp.c:362-370` | The refusal of a request that carries no session identifier tests the request type as a bitmask over sequential enumerators, so it fires only for GET_PARAMETER, SET_PARAMETER and RECORD and issues ANNOUNCE, PLAY, PAUSE and TEARDOWN with no session identifier at all. | reproduce | `curl-proto-rtsp` (planned) | Per-method matrix asserting for which request types the refusal fires, anchored on upstream case 568, which issues two ANNOUNCE requests with no session identifier and expects them on the wire. | obligation | DL-0180 |
| HZ-0153 | `original/lib/rtsp.c:463-473` | The range guard tests the request type as a bitmask over sequential enumerators, so a Range header is emitted for every request type that reaches it and not only for PLAY, PAUSE and RECORD. | reproduce | `curl-proto-rtsp` (planned) | Per-method matrix asserting the Range header appears for every request type when CURLOPT_RANGE is set; no upstream case sets a range over RTSP, so the matrix is project-authored. | obligation | DL-0180 |
| HZ-0154 | `original/lib/smb.c:685-688` | Session setup ignores both hash-construction results, so an allocation failure inside the NT hash leaves its 21-byte buffer indeterminate and those bytes become three DES keys and 24 bytes of the setup body. | diverge | `curl-proto-smb` (planned) | Fault-injected hash-construction failure asserting the deterministic out-of-memory outcome, plus the divergence-allowlist entry. | obligation | DL-0181 |
| HZ-0155 | `original/lib/smb.c:532-566` | A response frame is length-checked but its four-byte signature is never compared, so a reply carrying a foreign signature is processed as a valid message and the transfer completes. | reproduce | `curl-proto-smb` (planned) | Differential test over a negotiate reply carrying a foreign signature, asserting the same body, exit code and stderr. | obligation | DL-0181 |
| HZ-0156 | `original/lib/smb.c:1090-1117` | The download stays in its read state while a chunk is 0x8000 or larger, and the body reaches the write callback in maximum-write-size pieces rather than in the sizes the peer sent. | reproduce | `curl-proto-smb` (planned) | Differential harness comparing the read request count and the write-callback invocation sizes for a body larger than one payload. | obligation | DL-0181 |
| HZ-0157 | `original/lib/smb.c:829-848` | The upload clamps the remaining size to one byte below the maximum payload before narrowing it to sixteen bits, and the byte count it declares is that length plus one byte of padding. | reproduce | `curl-proto-smb` (planned) | Differential test over an upload larger than one payload asserting the wire data-length and byte-count sequence. | obligation | DL-0181 |
| HZ-0158 | `original/lib/smb.c:936-951` | The challenge is copied eight bytes at a time out of a one-byte trailing member, and the size test subtracts that member's byte to admit a frame the declared structure does not cover. | substitute | `curl-proto-smb` (planned) | Equivalence test over negotiate replies at the minimum challenge length and one byte below it, asserting the boundary decision. | obligation | DL-0181 |
| HZ-0159 | `original/src/tool_urlglob.h:62-63` | The header states a pattern bound of 30 that nothing at the tag reads, so the observable ceiling is the one the pattern-array append imposes: its growth branch runs only when the used count reaches the allocated count, that allocation doubles from two, and the guard inside the branch admits a count below 255, so the guard is evaluated only at powers of two and the pattern raising the count to 256 is the first one refused with the malformed-URL code. The count includes every literal run as well as every brace and bracket glob, so the refusal names brace sets while counting more than those. | reproduce | `curl-cli` (planned) | Differential comparison across the boundary in both shapes, at 127 and 128 repetitions of a set followed by a literal and at 254 and 255 repetitions of a bare set, asserting exit code, message text and reported position, together with upstream case 761. | obligation | DL-0182 |
| HZ-0160 | `original/src/tool_cb_see.c:41-77` | The chunked large-offset path answers three conditions with the code that fails the whole transfer - a whence other than SEEK_SET, a failed rewind to the start and a failed forward step - so on a build whose curl_off_t is wider than off_t an offset above 0x7FFFFFFE on a descriptor that cannot seek ends the transfer, while the single seek call outside that path answers the identical failure of the identical call with the cannot-seek code and lets libcurl fall back to re-reading. | reproduce | `curl-cli` (planned) | Equivalence test over the tool's seek callback in both configurations of the width condition, asserting the cannot-seek code for a small offset and the transfer-abort code for an offset above the guard on the same unseekable descriptor, and asserting both configurations agree on the seekable path. | obligation | DL-0183 |
| HZ-0161 | `original/src/tool_formparse.c:383-424` | A quoted word whose closing quote is missing is re-parsed from the opening quote as an unquoted word, so the leading double quote stays in the value and no diagnostic is printed. | reproduce | `curl-cli` (planned) | Differential comparison of the emitted part name, filename and body for a form argument with an unbalanced quote, asserting the retained quote character in both trees. | obligation | DL-0188 |
| HZ-0162 | `original/src/tool_formparse.c:386-408` | Inside a quoted word only a backslash before another backslash or before a double quote is removed, and the unescaping rewrites the buffer in place starting at the first escape, so every other backslash sequence reaches the part verbatim. | reproduce | `curl-cli` (planned) | Differential comparison over quoted filenames carrying each backslash sequence, including a trailing lone backslash and a backslash before a semicolon. | obligation | DL-0188 |
| HZ-0163 | `original/src/tool_formparse.c:409-419` | Text between a quoted word's closing quote and the next semicolon or delimiter is skipped, reported only when it holds a non-space character, and never becomes part of the value. | reproduce | `curl-cli` (planned) | Differential stderr and request-body comparison for a quoted value followed by stray text, and for one followed only by spaces, asserting the warning appears in the first case alone. | obligation | DL-0188 |
| HZ-0164 | `original/src/tool_formparse.c:689-698` | Trailing blanks are stripped from a parsed word only when the parser hands back the pointer it was given, which holds for an unquoted word and fails for a quoted one, so a quoted value keeps its trailing spaces and an unquoted value loses them. | reproduce | `curl-cli` (planned) | Differential comparison of one unquoted and one quoted value that both end in two spaces, across the data, filename, content-type and encoder positions. | obligation | DL-0188 |
| HZ-0165 | `original/src/tool_formparse.c:699-733` | A content-type sub-option is admitted only while no content type is pending, so a second consecutive one is folded into the first value as unrecognised text, while one that follows any other sub-option is admitted again and replaces the earlier content type. | reproduce | `curl-cli` (planned) | Differential comparison of the emitted content type for two consecutive type sub-options, and for a type sub-option separated from an earlier one by a filename sub-option. | obligation | DL-0188 |
| HZ-0166 | `original/src/tool_formparse.c:720-728` | An unrecognised sub-option is consumed as a word, reported as a skipped form field only when it is non-empty, and the argument is still accepted. | reproduce | `curl-cli` (planned) | Differential exit-code and stderr comparison for an unknown sub-option, an empty one and a name-shaped one, asserting both trees accept the argument. | obligation | DL-0188 |
| HZ-0167 | `original/src/tool_formparse.c:857-869` | Starting a multipart group parses the word between the opening parenthesis and the first sub-option and then never reads it, and that text is discarded with no diagnostic. | reproduce | `curl-cli` (planned) | Differential request-body comparison for a group whose opening argument carries text before the first sub-option, asserting the text appears nowhere in either body. | obligation | DL-0188 |
| HZ-0168 | `original/src/tool_formparse.c:870-877` | A group is terminated only by an argument with an empty name whose whole value is one closing parenthesis; the same value under a name becomes an ordinary data part, and a terminator at the root warns and fails the argument. | reproduce | `curl-cli` (planned) | Differential comparison over a bare terminator, a named terminator, a terminator with trailing text and a terminator at the root, asserting body, stderr and exit code. | obligation | DL-0188 |
| HZ-0169 | `original/src/tool_formparse.c:577-591` | A headers file that cannot be opened is reported as a warning and the part is still built without those headers, so a mistyped headers path does not fail the argument or the transfer. | reproduce | `curl-cli` (planned) | Differential stderr, exit-code and request-header comparison for a headers file that is absent and for one that is unreadable. | obligation | DL-0188 |
| HZ-0170 | `original/src/tool_formparse.c:455-493` | A continuation line is folded onto the previous collected header only when one already exists, so a headers file whose first line begins with a space contributes that line as a header of its own, and comment and empty lines are dropped before the test. | reproduce | `curl-cli` (planned) | Differential request-header comparison over a headers file that opens with a continuation line, one that opens with a comment and one whose folds follow a real header. | obligation | DL-0188 |
| HZ-0171 | `original/src/tool_formparse.c:562-568` | A headers sub-option introduced by a less-than sign reads from a file exactly as one introduced by an at sign, although those two characters select different part kinds at the top level of the argument. | reproduce | `curl-cli` (planned) | Differential request-header comparison for the same headers file named with an at sign and with a less-than sign. | obligation | DL-0188 |
| HZ-0172 | `original/src/tool_formparse.c:978-981` | Unconsumed text after a field specification has its separator restored, is reported, and is then ignored, so the field is still accepted and the text reaches no part. | reproduce | `curl-cli` (planned) | Differential stderr, exit-code and request-body comparison for a data field followed by unconsumed text. | obligation | DL-0188 |
| HZ-0173 | `original/src/tool_formparse.c:268-272` | The part-count ceiling is tested before the counter is incremented, so a sibling list one longer than the stated maximum is still translated and only the next one answers with the bad-argument code. | reproduce | `curl-cli` (planned) | Boundary test at the stated maximum, at one above it and at two above it, asserting which counts translate and which return the bad-argument code. | obligation | DL-0188 |
| HZ-0174 | `original/src/tool_formparse.c:78-94` | The size-to-offset conversion clears the top bit of its argument and asserts only in a debug build, so a size above the signed maximum is reported as a smaller positive size in a release build instead of being refused. | substitute | `curl-cli` (planned) | Equivalence test over the conversion at zero, at the signed maximum and one above it, asserting the checked conversion agrees for every reachable input. | obligation | DL-0188 |
| HZ-0175 | `original/src/tool_formparse.c:127-139` | When standard input is a regular file whose reported size is below the current offset, the part size is clamped to zero and an empty body is sent in place of an error, and the data is left in the stream and read on demand. | reproduce | `curl-cli` (planned) | Differential request-body comparison for standard input redirected from a regular file already positioned past its end, and for one positioned mid-file. | obligation | DL-0188 |
| HZ-0176 | `original/src/tool_formparse.c:140-160` | Buffering standard input releases a zero-length buffer inside the helper, and the parser then allocates an empty string in its place, so an empty pipe yields an allocated empty part instead of a null one. | reproduce | `curl-cli` (planned) | Differential request-body comparison for an empty pipe on standard input, asserting an empty part with its headers is emitted by both trees. | obligation | DL-0188 |
| HZ-0177 | `original/src/tool_formparse.c:907-922` | On the remote-file branch a standard-input read error is raised at parse time only when some data was already buffered; otherwise the buffer is released, the size is set to minus one and the error is cleared for the library to meet later. | reproduce | `curl-cli` (planned) | Fault-injected read failure on standard input with and without buffered data, asserting the parse-time failure in the first case and the deferred outcome in the second. | obligation | DL-0188 |
| HZ-0178 | `original/src/tool_formparse.c:939-955` | The plain-file branch repeats the same deferred handling of a standard-input read error independently of the remote-file branch, so both branches must answer alike for the same failure. | reproduce | `curl-cli` (planned) | The same fault-injected read failure driven through a less-than argument, asserting the two branches agree with each other and with the oracle. | obligation | DL-0188 |
| HZ-0179 | `original/src/tool_formparse.c:209-227` | A stream error while reading standard input warns and aborts the transfer through the read callback, and the comment claiming the error is shown once enforces nothing, so a caller that retries the callback is warned again. | reproduce | `curl-cli` (planned) | Differential stderr and exit-code comparison for a failing standard-input stream, asserting the abort result and the number of warning lines. | obligation | DL-0188 |
| HZ-0180 | `original/src/tool_formparse.c:311-327` | A remote-file part with no explicit filename keeps the name the library derives from the path, a plain-file part with none has its filename explicitly cleared, and a standard-input part with none is named with a single dash through a deliberate fall-through. | reproduce | `curl-cli` (planned) | Differential request-body comparison of the emitted filename parameter for the at-sign, less-than and dash forms, each with and without an explicit filename sub-option. | obligation | DL-0188 |
| HZ-0181 | `original/src/tool_formparse.c:177-192` | Releasing the part tree recurses once per sibling as well as once per nesting level, so a form built from the maximum sibling count unwinds that many frames. | substitute | `curl-cli` (planned) | Release test over the maximum sibling count and over deep nesting, asserting no leak and no stack exhaustion for a shape the oracle accepts. | obligation | DL-0188 |
| HZ-0182 | `original/src/tool_formparse.c:757-758` | The terminator is returned masked to its low eight bits, which keeps a high-bit delimiter byte from colliding with the negative error sentinel and reports it as its unsigned value. | reproduce | `curl-cli` (planned) | Equivalence test over terminator bytes at zero, at a comma, at the high-bit boundary and on the error path, asserting the sentinel stays distinguishable. | obligation | DL-0188 |
| HZ-0183 | `original/src/tool_operate.c:1353-1467` | Header-callback setup stores the address of a block-scope etag structure into per-transfer state that outlives the enclosing call, beside two pointers duplicating per-transfer members, and no read site consumes any of the three. | substitute | `curl-cli` (planned) | Field-propagation audit asserting the three write-only members carry reserved markers rather than read sites, plus the differential command-line corpus over etag saving, header dumping and remote header naming in serial and parallel transfers. | obligation | DL-0189 |
| HZ-0184 | `original/src/tool_cb_rea.c:80-148` | The running upload total that both the completion check and the over-read clamp read is never advanced anywhere in the tree, so it stays at the zero its structure was allocated with: the completion check can only fire for a declared size of zero, and the clamp measures each single read against the whole declared size and then clamps that read to the whole declared size rather than to the bytes still outstanding, while the warning it prints reports one read's excess as an end-of-transfer overshoot. | reproduce | `curl-cli` (planned) | Differential upload comparison over both trees for a declared size smaller than one read, asserting the same clamped byte count reaches the wire and the same warning text and byte figure reach stderr, together with a zero-length upload asserting the completion check fires identically. | obligation | DL-0191 |
| HZ-0185 | `original/src/tool_cb_rea.c:159-188` | The previous upload count that decides whether the unpause path waits a millisecond is one function-static shared by the whole process, while the callback holding it is installed on every standard-input upload handle, so concurrent transfers compare against whatever transfer wrote it last and it is never reset between transfers, leaving the wait taken or skipped on another transfer's progress. | reproduce | `curl-cli` (planned) | Differential comparison of concurrent standard-input uploads in parallel mode over both trees, asserting identical stdout, stderr, exit code and written bytes, plus a test asserting the previous-count state is process-wide rather than per-transfer. | obligation | DL-0191 |
| HZ-0186 | `original/src/tool_cb_wrt.c:314-323` | The binary-output refusal is armed only while fewer than 2000 bytes have been counted for the destination and fires only on a NUL byte in the chunk being written, so a body whose first NUL falls past that window is written to the terminal unrefused and up to 1999 bytes of a binary body are already written before the refusal can fire. | reproduce | `curl-cli` (planned) | Differential command-line rows over a simulated terminal with the first NUL inside the window, at its edge and past it, asserting exit code, written bytes and stderr, plus upstream cases 1425 and 1426. | obligation | DL-0194 |
| HZ-0187 | `original/src/tool_cb_wrt.c:316-322` | The refusal suppresses the tool's own error line by setting the synthetic-error flag, and the warning itself is suppressed by --silent, so a silent invocation that refuses binary output exits 23 with no diagnostic output at all. | reproduce | `curl-cli` (planned) | Differential row asserting exit 23 with an empty stderr under --silent and the three-line warning without it. | obligation | DL-0193 |
| HZ-0188 | `original/src/tool_cb_wrt.c:61-81` | The no-clobber walk over numbered names increments its counter before opening and stops once it reaches one hundred, so the last name it attempts is a dot ninety-nine while the option's own manual page documents appending numbers up to a dot one hundred. | reproduce | `curl-cli` (planned) | Differential row with the base name and ninety-nine numbered files present and the hundredth free, asserting the failure and that the hundredth name is never created. | obligation | DL-0195 |
| HZ-0189 | `original/src/tool_cb_wrt.c:82-104` | When the numbered walk is exhausted the output filename has already been replaced by the last name attempted, a file this run never created, while the failure diagnostic still names the base file and reports the errno of that last attempt. | reproduce | `curl-cli` (planned) | Differential row asserting the stderr text names the base file, plus an equivalence assertion that the replaced name is never removed because removal is gated on the file having been opened here. | obligation | DL-0195 |
| HZ-0190 | `original/src/tool_cb_wrt.c:341-366` | The value returned to the library is the item count the buffered write reports while the value it is compared against is the byte count, which agree only because the write callback contract fixes the item size at one. | substitute | `curl-cli` (planned) | Equivalence test over a short write asserting the returned relation, driven against a destination that accepts part of a chunk so the library reports the passed and returned counts. | obligation | DL-0195 |
| HZ-0191 | `original/src/tool_cb_wrt.c:325-342` | The buffered header list is flushed only on the branch that does not write to a Windows console, so headers held back while a Content-Disposition name was awaited never reach a console destination and are released later without being written. | reproduce | `curl-cli` (planned) | Equivalence assertion over the branch order on the console path, compiled and linked for Windows and recorded as deferred there, plus a differential row on the non-console path. | obligation | DL-0192 |
| HZ-0192 | `original/src/tool_cb_wrt.c:203-235` | The console writer reports the whole chunk as written even when it deferred the trailing one to three bytes of an incomplete sequence into the per-destination carry buffer, and even when the conversion silently dropped input it could not encode, so the byte count includes bytes the console never received. | reproduce | `curl-cli` (planned) | Equivalence test over a chunk boundary splitting a multi-byte sequence, asserting the reported count and the carry buffer, compiled and linked for Windows and recorded as deferred there. | obligation | DL-0192 |
| HZ-0193 | `original/src/tool_cb_wrt.c:348-351` | The read side is unpaused from inside the write callback with its result discarded, and the busy flag is cleared before the call, so a failed unpause leaves the read side paused with nothing left to retry it. | reproduce | `curl-cli` (planned) | Equivalence test over an upload from standard input whose read callback reported busy, asserting the unpause happens once per busy report and that the outcome is not consulted. | obligation | DL-0195 |
| HZ-0194 | `original/src/tool_cb_wrt.c:353-363` | The unbuffered-output flush is retried while errno reads as interrupted, although a failing flush need not have set errno and may already have written part of the stream, and upstream disables its own errno check over the loop. | reproduce | `curl-cli` (planned) | Equivalence test over a flush that fails without setting errno and one that reports interruption, asserting a single write error in the first case and a retry in the second. | obligation | DL-0195 |
| HZ-0195 | `original/src/tool_cb_wrt.c:268-279` | The debug-only write ceiling is chosen by whether headers are being shown rather than by what the data is, so in a debug build a body write is measured against the body limit and any write is refused above it while a release build accepts every one of them. | reproduce | `curl-cli` (planned) | Equivalence test over both ceilings asserting the refusal text and code under the debug feature and their absence without it. | obligation | DL-0192 |
| HZ-0196 | `original/src/tool_cb_wrt.c:259-266` | The debug-only terminal simulation reads its environment variable through the reader that collapses a set-but-empty value to not-set, so an exported empty value does not arm the refusal while any non-empty value does. | reproduce | `curl-cli` (planned) | Equivalence test over the variable unset, set empty and set non-empty, asserting which of the three arms the refusal. | obligation | DL-0194 |
| HZ-0197 | `original/src/tool_cb_wrt.c:250-250` | The terminal flag the refusal depends on is one process-wide value written while a transfer is created from that transfer's already-open stream and restored while the next one is created, and it is read here when a body is written, so in a parallel invocation a transfer bound to a regular file is refused because a sibling was bound to a terminal. | reproduce | `curl-cli` (planned) | Differential row driving parallel transfers with mixed destinations under a pseudo-terminal, asserting that the file-bound transfer fails and its file is left empty, against the same command line without parallel mode. | obligation | DL-0194 |
| HZ-0198 | `original/src/tool_cb_wrt.c:344-346` | The written-byte counter advances only when the whole chunk was written, so after a short write the destination holds bytes the counter does not carry, which is why the auto-resume predicate in the caller excludes a write error outright. | reproduce | `curl-cli` (planned) | Equivalence test over a short write asserting the counter is unchanged, plus a differential retry row asserting the truncation target. | obligation | DL-0195 |
| HZ-0199 | `original/src/tool_urlglob.c:407-425` | The pattern-slot cap counts every literal run as well as every set and every range, and the failure at the 256th slot reports the too-many-brace-sets message at the running column. | reproduce | `curl-cli` (planned) | Differential comparison over a URL of repeated set-and-literal pairs asserting the reported message, error code and position. | obligation | DL-0198 |
| HZ-0200 | `original/src/tool_urlglob.c:411-413` | The recorded slot capacity is doubled before the cap is tested, so the failing path leaves a capacity recorded that the allocation never reached. | substitute | `curl-cli` (planned) | Equivalence test over the cap failure asserting the reported code and that no expansion is produced. | obligation | DL-0198 |
| HZ-0201 | `original/src/tool_urlglob.c:552-568` | The error report is formatted into a 512-byte buffer, so a long URL is truncated part way and the caret line is dropped from the output entirely. | reproduce | `curl-cli` (planned) | Byte comparison of the error stream over a URL long enough to fill the formatting buffer. | obligation | DL-0199 |
| HZ-0202 | `original/src/tool_urlglob.c:557-560` | The caret line is indented by the reported position minus one, taken as a signed width from an unsigned position, and a range rejected at the end of a URL reports a position one past its last byte. | reproduce | `curl-cli` (planned) | Byte comparison of the error stream over a bad range at the end of the URL. | obligation | DL-0199 |
| HZ-0203 | `original/src/tool_urlglob.c:523-529` | The duplicate-name failure reports a zero-based offset from the start of the pattern, while every other failure in the unit reports the one-based running column. | reproduce | `curl-cli` (planned) | Byte comparison of the error stream over a pattern that sets one glob name twice. | obligation | DL-0199 |
| HZ-0204 | `original/src/tool_urlglob.c:366-405` | Bracket-group classification ignores the hyphen and colon rule stated in its own comment and accepts whatever the URL parser accepts for a bracketed host with a guessed scheme. | reproduce | `curl-cli` (planned) | URL corpus over bracket groups covering IPv6 literals, zone identifiers and numeric ranges, comparing the accept decision and every parsed component. | obligation | DL-0200 |
| HZ-0205 | `original/src/tool_urlglob.c:383-386` | The classifier's 128-byte bound sits above the longest bracket group the URL parser accepts, so no group that reaches the bound is ever classified as a literal and every such group is rejected by the range scanner instead. | reproduce | `curl-cli` (planned) | Corpus pair at 127 and 128 bytes asserting the identical bad-range outcome on both sides of the bound, plus a zone identifier at 15 and 16 bytes asserting where acceptance actually stops. | obligation | DL-0200 |
| HZ-0206 | `original/src/tool_urlglob.c:459-475` | An empty bracket pair is copied into the literal run, and so is any bracket group the classifier accepts, so neither opens a range; inside a set the same opening bracket is instead rejected as a nested expression. | reproduce | `curl-cli` (planned) | Differential over URLs carrying an empty bracket pair, an IPv6 literal and a bracket inside a brace set, the last being the example the globoff documentation itself gives. | obligation | DL-0200 |
| HZ-0207 | `original/src/tool_urlglob.c:480-492` | Outside a set the escape byte is consumed only when a brace or a bracket follows it, and before any other byte it is copied as data. | reproduce | `curl-cli` (planned) | Differential over escaped and unescaped special characters in the literal part of a URL. | obligation | DL-0197 |
| HZ-0208 | `original/src/tool_urlglob.c:177-189` | Inside a brace set the escape byte consumes whatever byte follows it, including a comma, a closing brace and a second escape byte. | reproduce | `curl-cli` (planned) | Differential over set elements carrying escaped separators and escaped escape bytes. | obligation | DL-0197 |
| HZ-0209 | `original/src/tool_urlglob.c:303-311` | The zero-padding width of a numeric range is the digit count of its low bound, so a leading zero on that bound pads every value while values wider than the bound are emitted unpadded. | reproduce | `curl-cli` (planned) | Differential over ranges whose bounds differ in digit count and over a literal zero preceding a range. | obligation | DL-0197 |
| HZ-0210 | `original/src/tool_urlglob.c:333-337` | A step wider than the span, and any step other than one where the bounds are equal, is rejected as a bad range rather than yielding a single value. | reproduce | `curl-cli` (planned) | Range matrix over equal bounds with steps of one and more than one, and over steps wider than the span. | obligation | DL-0197 |
| HZ-0211 | `original/src/tool_urlglob.c:313-329` | Blanks are skipped between the hyphen and the high bound of a numeric range and at no other position in it. | reproduce | `curl-cli` (planned) | Corpus over blanks at each of the four positions of a numeric range. | obligation | DL-0197 |
| HZ-0212 | `original/src/tool_urlglob.c:258-266` | An alphabetic step is parsed with a ceiling of 256 and then narrowed to eight bits, so a step of 256 becomes zero and the range is rejected as bad. | reproduce | `curl-cli` (planned) | Corpus over alphabetic steps of one, 255, 256 and 257. | obligation | DL-0197 |
| HZ-0213 | `original/src/tool_urlglob.c:277-282` | An alphabetic range spanning more than 25 letters is rejected, so a range running from an upper-case letter to a lower-case one is a bad range. | reproduce | `curl-cli` (planned) | Corpus over lower-case, upper-case and case-crossing alphabetic ranges. | obligation | DL-0197 |
| HZ-0214 | `original/src/tool_urlglob.c:134-137` | A brace set is capped at 100000 elements and the cap is reported with the range-overflow message rather than one naming sets, and it is reported with a position of zero, so the rendered error carries neither a position nor a caret line unlike the same message raised from a range. | reproduce | `curl-cli` (planned) | Differential at the set-element cap asserting the reported message, the absent position and the code, against the same message raised from a range. | obligation | DL-0197 |
| HZ-0215 | `original/src/tool_urlglob.c:67-88` | The expansion-count product yields zero for a non-positive factor, which a debug build stops on and a release build accepts silently. | substitute | `curl-cli` (planned) | Equivalence test over the expansion count at the overflow boundary and at a zero factor. | obligation | DL-0197 |
| HZ-0216 | `original/src/tool_urlglob.c:339-349` | The high bound plus the step and the product of the expansion counts are overflow-checked separately, and both rejections are reported as a malformed URL. | reproduce | `curl-cli` (planned) | Differential over a range ending at the largest representable value and over two ranges whose product overflows. | obligation | DL-0197 |
| HZ-0217 | `original/src/tool_urlglob.c:784-806` | The failure returns release nothing because a failed append has already released the accumulator inside the buffer implementation, and on success the non-Windows path hands that same allocation to the caller without copying it while the Windows path releases it and returns a sanitized copy. | substitute | `curl-cli` (planned) | Leak accounting under the debug allocator feature over a fault-injected append failure, plus an equivalence test asserting the returned name and its ownership on both the Windows and the non-Windows path. | obligation | DL-0202 |
| HZ-0218 | `original/src/tool_urlglob.c:719-736` | A numbered reference in an output name is bounded by the pattern-slot count rather than the number of globs and is then matched on the glob index, so a zero, an out-of-range and an unmatched number are all copied literally. | reproduce | `curl-cli` (planned) | Differential over a zero reference, a reference past the glob count and a reference landing on a literal slot. | obligation | DL-0203 |
| HZ-0219 | `original/src/tool_urlglob.c:737-760` | A well-formed named reference with no matching glob in either list fails with the bad-function-argument code, while a malformed one is copied literally. | reproduce | `curl-cli` (planned) | Differential over a missing glob name and over a name with no closing angle bracket. | obligation | DL-0203 |
| HZ-0220 | `original/src/tool_urlglob.c:513-522` | A malformed or over-long name after the opening character is not reported as an error: the bytes are reinterpreted as pattern content. | reproduce | `curl-cli` (planned) | Differential over names longer than 64 bytes and names without a closing angle bracket. | obligation | DL-0201 |
| HZ-0221 | `original/src/tool_urlglob.c:513-515` | A glob name may carry any byte other than the closing angle bracket, up to 64 bytes, although the documentation describes the name as alphanumeric. | reproduce | `curl-cli` (planned) | Differential over names carrying blanks and punctuation, plus the documentation cross-check against original/docs/cmdline-opts/_GLOBBING.md. | obligation | DL-0201 |
| HZ-0222 | `original/src/tool_urlglob.c:570-596` | A failed parse reports an expansion count of one through the count output while returning the failure. | reproduce | `curl-cli` (planned) | Equivalence test asserting the reported count and code for a rejected URL. | obligation | DL-0197 |
| HZ-0223 | `original/src/tool_urlglob.c:39-61` | A literal run becomes a single-element set carrying a glob index of minus one, so it occupies a pattern slot that no reference can name. | reproduce | `curl-cli` (planned) | Equivalence test over slot accounting asserting that a reference landing on a literal slot is copied literally. | obligation | DL-0198 |
| HZ-0224 | `original/src/tool_urlglob.c:108-112` | An unterminated brace set reports the position reached at the end of the pattern rather than the position of the opening brace it could not match. | reproduce | `curl-cli` (planned) | Byte comparison of the error stream over an unterminated set, whose upstream case asserts the exit code alone. | obligation | DL-0199 |
| HZ-0225 | `original/src/tool_urlglob.c:703-714` | An output name that grows past one mebibyte fails with the too-large code and sets no glob error string, so the tool reports it as a warning about a bad output glob rather than with the position and caret every other glob failure carries. | reproduce | `curl-cli` (planned) | Differential over an output name longer than one mebibyte asserting the exit code and the exact error stream. | obligation | DL-0202 |
| HZ-0226 | `original/src/tool_urlglob.c:118-132` | Only a brace pair with nothing at all between its braces is rejected as an empty string within braces; an empty element beside a comma is accepted, so a set can carry empty elements, produce duplicate expansions and yield an empty output name. | reproduce | `curl-cli` (planned) | Differential over an empty brace pair, an empty leading element, an empty trailing element and a set of nothing but empty elements, asserting the expansions and the reported codes. | obligation | DL-0197 |
| HZ-0227 | `original/src/tool_cb_soc.c:47-55` | Where the Linux arm is not taken, the open-socket callback answers every stream request with the bad-socket value and leaves the socket error untouched, so the transfer is abandoned on every resolved address and the library's failure line renders whatever error value was already set. | reproduce | `curl-cli` (planned) | Platform-arm equivalence test asserting the refusal answers the bad-socket value with the socket error unchanged, plus a differential exit-code and stderr comparison of the Multipath TCP option on a build where that arm is selected. | obligation | DL-0207 |
| HZ-0228 | `original/src/tool_cb_soc.c:47-57` | On the Linux arm the Multipath TCP protocol number reaches the socket call with no fallback, so a kernel that refuses it fails the whole transfer with a connect error rather than retrying as a plain stream socket, while datagram requests pass through untouched. | reproduce | `curl-cli` (planned) | Differential exit-code and stderr comparison of the Multipath TCP option against a host whose kernel refuses the protocol number, asserting both trees abandon the transfer, and a datagram pass-through assertion over the same callback. | obligation | DL-0207 |
| HZ-0229 | `original/src/tool_operhlp.c:199-219` | The remote filename is taken from the still percent-encoded URL path, so an escaped byte reaches the filesystem verbatim and a URL ending in a%2Fb writes a file literally named a%2Fb. | reproduce | `curl-cli` (planned) | Differential command-line corpus over -O URLs whose last segment carries percent escapes, comparing the created filename bytes against the baseline binary. | obligation | DL-0209 |
| HZ-0230 | `original/src/tool_operhlp.c:203-215` | A backslash after the last slash of the URL path is treated as a directory separator on every platform, so a path segment that is a legal single filename on POSIX is truncated at the backslash and the part before it is discarded. | reproduce | `curl-cli` (planned) | Differential command-line corpus over -O URLs carrying a backslash in the last path segment, comparing the created filename against the baseline binary on both execution-verified platforms. | obligation | DL-0209 |
| HZ-0231 | `original/src/tool_operhlp.c:203-215` | The derivation truncates the buffer the URL API handed out in place and repeats without bound until no trailing separator is left, so the name is read out of a mutated copy of the path. | substitute | `curl-cli` (planned) | Equivalence test over paths ending in one, two and seventeen separators plus a separator-only path, asserting the derived name of each against the baseline binary. | obligation | DL-0209 |
| HZ-0232 | `original/src/tool_operhlp.c:221-226` | A path with no separator left yields the fixed literal name curl_response, which truncates any existing file of that name, and the only signal is the stderr line Warning: No remote filename, uses "curl_response", itself suppressed by --silent. | reproduce | `curl-cli` (planned) | Differential comparison of stdout, stderr and written filenames over a separator-only URL with and without --silent, plus the upstream cases 690, 692 and 1639. | obligation | DL-0209 |
| HZ-0233 | `original/src/tool_operhlp.c:232-241` | Filename sanitisation is compiled only for Windows and MSDOS, so on POSIX the derived name reaches the filesystem unsanitised, leading dashes and colons included, and the sanitise status stays SANITIZE_ERR_OK. | reproduce | `curl-cli` (planned) | Differential command-line corpus over -O URLs whose last segment begins with a dash or holds a colon on Linux, with the Windows arm recorded as deferred under the platform matrix. | obligation | DL-0209 |
| HZ-0234 | `original/src/tool_operhlp.c:104-113` | Any query string in the upload URL suppresses the local filename append entirely, so an upload to a path ending in a slash keeps that directory path as its request target. | reproduce | `curl-cli` (planned) | Differential comparison of the request target for -T against a directory path with and without a query string. | obligation | DL-0210 |
| HZ-0235 | `original/src/tool_operhlp.c:121-133` | The local basename appended to the upload URL is cut at the rightmost slash and then at the rightmost backslash after it, so a POSIX filename containing a backslash loses everything before that backslash before percent-encoding. | reproduce | `curl-cli` (planned) | Differential comparison of the request target for -T over local names carrying a backslash, a space and a percent character. | obligation | DL-0210 |
| HZ-0236 | `original/src/tool_operhlp.c:34-48` | Releasing one operation config's URL list also clears the global URL-glob, upload-glob and upload-filename state that the parameter does not name, and the teardown loop calls it once per leftover transfer. | reproduce | `curl-cli` (planned) | Leak and double-release accounting under the debug allocator feature over a teardown with several leftover transfers sharing one config, plus a globbed multi-URL run. | obligation | DL-0211 |
| HZ-0237 | `original/src/tool_operhlp.c:53-58` | Whether an upload expects output is decided by a literal prefix test on the URL string, so a scheme-less URL that curl itself resolves to HTTP answers no and leaves the progress meter drawn over the response body on a terminal. | reproduce | `curl-cli` (planned) | Pseudo-terminal differential over the same successful upload written with and without an explicit http:// prefix, comparing stderr bytes. | obligation | DL-0211 |
| HZ-0238 | `original/src/tool_operhlp.c:61-64` | Both - and . select standard input here, while the non-blocking mode that . additionally selects is decided by four separate literal comparisons elsewhere in the tool. | reproduce | `curl-cli` (planned) | Differential comparison of the request target and read behaviour for -T - against -T . , asserting both take the standard-input path and only the latter reads non-blocking. | obligation | DL-0211 |
| HZ-0239 | `original/src/tool_operhlp.c:69-77` | Every URL API failure outside the four named codes becomes CURLE_URL_MALFORMAT, so a bad port, a bad host character and a bad percent escape are indistinguishable in both the exit code and the message. | reproduce | `curl-cli` (planned) | Differential command-line corpus over malformed URLs asserting the exit code and the stderr text of each cause against the baseline binary. | obligation | DL-0211 |
| HZ-0240 | `original/src/tool_parsecfg.c:49-73` | A backslash at the very end of a quoted value is consumed and produces no character: the arm for the terminating byte continues the loop, which re-tests that terminator and ends the value, so the backslash disappears and nothing is reported. | reproduce | `curl-cli` (planned) | Differential comparison over a config line whose quoted value ends in a backslash, asserting the value both trees hand the option parser and that neither writes a diagnostic. | obligation | DL-0215 |
| HZ-0241 | `original/src/tool_parsecfg.c:53-69` | A backslash before any character other than t, n, r or v emits that character verbatim, so a doubled backslash yields one backslash and an escaped double quote yields a double quote that does not close the value, and a literal backslash can be written no other way. | reproduce | `curl-cli` (planned) | Differential comparison over one quoted value per escape the grammar names plus an unlisted escape, asserting the decoded bytes match in both trees. | obligation | DL-0215 |
| HZ-0242 | `original/src/tool_parsecfg.c:48-77` | A quoted value whose closing double quote is missing is accepted in silence: the copy loop ends at the end of the line and reports success, so the remainder of the line becomes the value with no diagnostic. | reproduce | `curl-cli` (planned) | Differential comparison over a config line whose quoted value is never closed, asserting the value taken and the absence of any diagnostic. | obligation | DL-0215 |
| HZ-0243 | `original/src/tool_parsecfg.c:148-190` | A quoted empty value reaches the option parser as a non-null empty string while an absent or blank value reaches it as a null pointer, so only the second lets a required-parameter check fire, and the empty string also passes the unused-parameter test unnoticed. | reproduce | `curl-cli` (planned) | Differential comparison of a quoted empty value against an omitted value for an option that requires one, asserting the exit code and stderr of both spellings. | obligation | DL-0215 |
| HZ-0244 | `original/src/tool_parsecfg.c:161-185` | An unquoted value ends at the first space, tab, carriage return or newline and the rest of the line is discarded; the warning about it fires only when the discarded text is not empty, a carriage return, a newline or a comment marker, so a trailing word warns while a trailing comment stays silent. | reproduce | `curl-cli` (planned) | Differential comparison over a trailing word and a trailing comment after an unquoted value, asserting the value taken and whether the warning is printed. | obligation | DL-0215 |
| HZ-0245 | `original/src/tool_parsecfg.c:34-36` | The equals sign and the colon count as separators only for an option written without a leading dash, so a dashed option and its value arrive as a single token; the equals form still works only because the option parser performs its own split on it, while the colon form reaches the option table as an unknown name. | reproduce | `curl-cli` (planned) | Differential comparison over dashed and undashed spellings of one option joined by an equals sign and by a colon, asserting exit code and stderr for all four. | obligation | DL-0215 |
| HZ-0246 | `original/src/tool_parsecfg.c:287-295` | A dashed option cannot carry a double-quoted value: the option-token scan swallows the opening quote into the option name, the text after the space becomes a parameter the option parser never consumes, and the run is abandoned for an unused parameter. | reproduce | `curl-cli` (planned) | Differential comparison of a dashed option given a double-quoted value, asserting the exit code and the option error text in both trees. | obligation | DL-0215 |
| HZ-0247 | `original/src/tool_parsecfg.c:156-160` | The leading single quote warning fires whenever the first character of a value is a single quote, and the value is then taken verbatim with that quote still part of it. | reproduce | `curl-cli` (planned) | Differential comparison over a value beginning with a single quote, asserting the warning text and the value the option receives. | obligation | DL-0215 |
| HZ-0248 | `original/src/tool_parsecfg.c:230-234` | The effective file name is replaced with the standard-input label when the file is opened, so the later comparison of that name against a single dash can never match and the substitution it guards is unreachable. | substitute | `curl-cli` (planned) | Differential stderr comparison of a failing config option read from standard input, asserting the standard-input label prefixes the message in both trees. | obligation | DL-0214 |
| HZ-0249 | `original/src/tool_parsecfg.c:340-346` | Each chunk's length comes from a string-length call, so a zero byte inside a config line truncates that line at the zero byte and discards the rest up to the newline, and a zero byte landing first in a chunk yields a zero length that breaks the reader and ends config parsing with no diagnostic. | reproduce | `curl-cli` (planned) | Differential comparison over a config file carrying a zero byte mid-line and one at a chunk boundary, asserting which options take effect and that neither tree reports anything. | obligation | DL-0216 |
| HZ-0250 | `original/src/tool_parsecfg.c:348-353` | A line beyond the ten-megabyte ceiling and a genuine out-of-memory raise the same flag and surface as the same read failure naming the config file, so a length problem and an allocation problem are indistinguishable. | reproduce | `curl-cli` (planned) | Differential comparison over a config line one byte past the ceiling, asserting the read-failure message and the exit code. | obligation | DL-0216 |
| HZ-0251 | `original/src/tool_parsecfg.c:148-152` | An allocation failure inside the quoted-value handler is reported as a usage error rather than an out-of-memory one, and the loop is left before any message is printed, so the run ends with that code and no output. | reproduce | `curl-cli` (planned) | Fault-injected allocation failure inside the quoted-value handler, asserting the returned code and that nothing is written to stderr. | obligation | DL-0215 |
| HZ-0252 | `original/src/tool_cb_prg.c:194-207` | The percentage rounds through a one-decimal conversion while the fill count truncates an independent floating-point product, so the two indicators disagree near the ends of the range: any fraction at or above 0.99950 renders 100.0 percent beside a bar still one character short of full, and any fraction below 0.0005 renders 0.0 percent beside an empty bar despite non-zero progress. | reproduce | `curl-cli` (planned) | Differential stderr byte comparison over rate-limited transfers whose callbacks land inside the top and bottom half-thousandth of the range, asserting both trees print the same percentage beside the same fill count. | obligation | DL-0219 |
| HZ-0253 | `original/src/tool_cb_prg.c:110-119` | The width selector clamps upward to a twenty-column minimum, so a terminal narrower than twenty columns is rendered a twenty-character frame that overflows and wraps rather than a frame that fits, and a width of exactly twenty takes the clamp arm because the comparison is strict. | reproduce | `curl-cli` (planned) | Differential stderr comparison driven under a pseudo-terminal whose window size is set to five, ten, twenty and twenty-one columns, asserting frame lengths of twenty, twenty, twenty and twenty-one in both trees. | obligation | DL-0219 |
| HZ-0254 | `original/src/tool_cb_prg.c:196-199` | The fill count is an unsigned conversion of a floating-point product whose left operand is a signed width less seven, so a width below seven would convert a negative double to an unsigned integer, and the only bound on the result is a clamp that the twenty-column minimum makes unreachable. | substitute | `curl-cli` (planned) | Equivalence test over the fill-count computation across every width the selector can produce, asserting a bounded count and no wrapped conversion at the lower edge. | obligation | DL-0219 |
| HZ-0255 | `original/src/tool_cb_prg.c:132-156` | When the recorded initial size is negative and neither direction reports a total, both the expected total and the current point are set to the maximum offset value, so the first invocation renders a complete bar at 100.0 percent and every later invocation returns early because the previous point already equals the current one. | reproduce | `curl-cli` (planned) | Equivalence test invoking the callback directly with a negative initial size and no totals, asserting one full frame followed by silence, since the command line cannot produce a negative resume offset. | obligation | DL-0219 |
| HZ-0256 | `original/src/tool_cb_prg.c:79-88` | The moving marker is copied into the frame before the four sine-driven fill characters are written, so those writes land on top of the marker and mutilate it whenever their positions coincide with it. | reproduce | `curl-cli` (planned) | Differential stderr comparison over an unknown-length transfer, asserting the same frames carry the same partially overwritten marker at the same offsets in both trees. | obligation | DL-0219 |
| HZ-0257 | `original/src/tool_cb_prg.c:158-182` | An unknown-length transfer short enough to complete in one callback draws no frame at all, because the animated branch is reached only after the first invocation and the sized branch is skipped for a zero total, yet the invocation counter is still incremented and another translation unit emits the closing newline on the strength of it, so the whole progress output is a single newline byte. | reproduce | `curl-cli` (planned) | Differential stderr byte comparison over a chunked response of a few bytes, asserting stderr is exactly one newline in both trees and that the counter reaches one. | obligation | DL-0219 |
| HZ-0258 | `original/src/tool_cb_prg.c:69-79` | The frame buffer is terminated at the current width while the marker offset was last bounded against the previous width, so a mid-transfer narrowing places the five-byte marker copy across the terminator and leaves the frame unterminated, and the write that emits it then reads past the intended end of the frame. | substitute | `curl-cli` (planned) | Equivalence test over the animated frame with the width reduced between invocations while the marker offset is at its upper bound, asserting a frame of exactly the new width and no read past it. | obligation | DL-0219 |
| HZ-0259 | `original/src/tool_cb_prg.c:76-112` | Two implicit sign conversions carry the terminal width: the frame fill converts a signed width to an unsigned length, and the width selector converts the unsigned result of the terminal lookup to a signed local, so either would change sign if its bound were ever lost, though at this tag the twenty-column floor and the ten-thousand-column ceiling keep both in range and the baseline build reports neither. | substitute | `curl-cli` (planned) | Equivalence test over the width selector and the frame fill across the whole range the terminal lookup can return, asserting an explicit checked conversion at each site and no sign change at either bound. | obligation | DL-0219 |

## Verdicts by owner

| Owner | Reproduce | Substitute | Diverge | Not ported | Total |
|---|---:|---:|---:|---:|---:|
| `curl-abi` | 1 | 0 | 0 | 0 | 1 |
| `curl-auth` | 8 | 10 | 0 | 0 | 18 |
| `curl-cli` | 95 | 12 | 0 | 0 | 107 |
| `curl-cookies` | 3 | 1 | 0 | 0 | 4 |
| `curl-core` | 37 | 4 | 0 | 0 | 41 |
| `curl-dns` | 10 | 3 | 4 | 0 | 17 |
| `curl-http1` | 13 | 0 | 0 | 0 | 13 |
| `curl-http2` | 6 | 1 | 2 | 0 | 9 |
| `curl-proto-file` | 1 | 1 | 1 | 0 | 3 |
| `curl-proto-ftp` | 4 | 2 | 0 | 0 | 6 |
| `curl-proto-mqtt` | 14 | 0 | 0 | 0 | 14 |
| `curl-proto-rtsp` | 2 | 0 | 0 | 0 | 2 |
| `curl-proto-smb` | 3 | 1 | 1 | 0 | 5 |
| `curl-proto-smtp` | 3 | 2 | 0 | 0 | 5 |
| `curl-proto-tftp` | 1 | 0 | 0 | 0 | 1 |
| `curl-types` | 5 | 7 | 0 | 0 | 12 |
| `none` | 0 | 0 | 0 | 1 | 1 |
