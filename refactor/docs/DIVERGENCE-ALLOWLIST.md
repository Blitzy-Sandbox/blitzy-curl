<!--
SPDX-FileCopyrightText: the blitzy-curl project contributors
SPDX-License-Identifier: curl
-->

# Divergence Allowlist

Generated by `cargo run --bin curl-audit-traceability`. Run it with
`--check` to compare this file against the tree; a difference, a gap in either
direction, an unresolved locator or a dangling pointer fails the run. Do not
edit this file by hand. Rationale lives in `refactor/docs/DECISION-LOG.md`.

Every behaviour where the port does not match the vendored oracle, with the
deterministic outcome it holds instead, why that is accepted and who is
accountable for it. **9 entry/entries**, one for each hazard in
`refactor/docs/QUIRKS.md` whose verdict is `diverge`.

The list is a gate in both directions: a `diverge` verdict with no entry here
and an entry here answering no `diverge` verdict each fail the run, as does an
entry whose owner disagrees with its hazard row or whose decision does not
resolve. Every `abidiff` suppression is an entry in this file rather than
tool configuration, so no suppression can hide a real difference. An empty
allowlist is the target; a growing one is the signal that the rewrite is
drifting and is what makes this file worth reading rather than filing.

## Entries

| ID | Hazard | Locator | Oracle behaviour | Outcome the port holds | Rationale | Owner | Decision |
|---|---|---|---|---|---|---|---|
| DV-0001 | HZ-0002 | `original/lib/fake_addrinfo.c:187-199` | An address-conversion allocation failure can return zero with a null result list, reporting a successful resolution that produced nothing. | The conversion answers the out-of-memory code and no address list; a caller never sees a successful resolution that produced nothing. | Safe Rust cannot express a successful return beside an absent result: the conversion answers a result type, so the empty-success state has no representation to reproduce. The chosen code is the one the allocation failure would have produced had it been carried, so every caller path already handles it. | `curl-dns` (planned) | DL-0113 |
| DV-0002 | HZ-0011 | `original/lib/file.c:395-419` | A failed status call leaves the buffer indeterminate, and the directory test later reads it outside the guard that recorded whether the call succeeded. | A failed status call answers not-a-directory, the outcome the zeroed buffer produces on the platforms in the matrix, and the buffer is never read outside the guard. | Reading an indeterminate buffer is undefined behaviour that safe Rust cannot perform, so the value the oracle reads cannot be reproduced. Not-a-directory is the deterministic choice: it keeps the transfer on the path the oracle takes whenever the buffer happens to be zeroed, which is what the upstream cases exercise. | `curl-proto-file` (planned) | DL-0113 |
| DV-0003 | HZ-0031 | `original/lib/asyn-ares.c:279-305` | The address conversion records an out-of-memory code in a local and returns only a pointer, so an allocation failure can surface as a successful empty or partial result. | The conversion answers the out-of-memory code, and a partial list is discarded rather than returned. | The oracle records the failure in a local it then discards, which safe Rust cannot mirror without inventing a second channel. Answering the code the oracle computed and dropped is the only outcome that is both deterministic and consistent with the code it wrote down. | `curl-dns` (planned) | DL-0113 |
| DV-0004 | HZ-0045 | `original/lib/doh.c:1139-1192` | A name-decode failure returns out of memory, because that is the initial value of the result variable rather than the cause. | A name-decode failure answers the decode error the parser produced rather than the out-of-memory code the oracle's uninitialised result variable carries. | Reproducing the oracle here would mean seeding a result variable with a code unrelated to the failure and letting it survive, which is the shape safe Rust removes by construction: the decode answers a result and the caller propagates it. The divergence is visible only in which error a malformed answer reports, never in whether the transfer fails. | `curl-dns` (planned) | DL-0119 |
| DV-0005 | HZ-0046 | `original/lib/doh.c:1139-1192` | A release build accepts one to three trailing bytes that the debug assertion rejects, so the two build types disagree about the same response. | Trailing bytes after a complete answer are rejected in every build, which is the debug build's behaviour, so one build type answers for both. | A port whose behaviour depends on whether assertions are compiled in would have to carry the release acceptance and the debug rejection as two contracts, and the harness runs debug builds, so the stricter of the two is the one every upstream case already asserts. Choosing the looser one would leave the assertion unreachable and the strictness untested. | `curl-dns` (planned) | DL-0119 |
| DV-0006 | HZ-0047 | `original/lib/cf-h2-proxy.c:179-196` | The peer-shutdown last-stream and error fields are declared and read but never assigned, so the values reported come from initialisation rather than from the frame. | The peer's shutdown last-stream and error values are taken from the frame the substitute library reports, so a non-zero field is reported as sent rather than as the initialisation value. | The replacement HTTP/2 implementation surfaces those fields through its own event, and discarding them to reproduce the oracle's unassigned locals would mean deliberately dropping data the substitute gives us. The tunnel outcome is unchanged either way, and the boundary difference is enumerated in the dependency-delta deliverable. | `curl-http2` (planned) | DL-0128 |
| DV-0007 | HZ-0073 | `original/lib/http2.c:1422-1485` | The promise store caps the number of header pointers it holds but places no limit on their total size. | The promise store bounds the total byte size of the headers it holds as well as their count, and answers the stream error the count limit already answers when either bound is exceeded. | An unbounded aggregate is a memory-exhaustion surface a peer controls, and the substitute library gives no equivalent cap, so reproducing the absence would import a denial of service the replacement cannot mitigate. The added bound is reported through the error the count limit already uses, so no new outcome appears; the difference is which promises are refused. | `curl-http2` (planned) | DL-0125 |
| DV-0008 | HZ-0154 | `original/lib/smb.c:685-688` | Session setup ignores both hash-construction results, so an allocation failure inside the NT hash leaves its 21-byte buffer indeterminate and those bytes become three DES keys and 24 bytes of the setup body. | A hash-construction failure answers the out-of-memory code and no session-setup message is sent; no key material is derived from an unwritten buffer. | The oracle turns an allocation failure into twenty-four bytes of indeterminate memory on the wire, derived from three DES keys built out of an unwritten buffer. Safe Rust cannot read an uninitialised buffer, and sending key material derived from one is a disclosure the port must not reproduce even bug for bug. Answering the failure is the only deterministic outcome that keeps the wire silent. | `curl-proto-smb` (planned) | DL-0181 |
| DV-0009 | HZ-0292 | `original/src/tool_doswin.c:714-734` | The Windows standard-input relay binds a loopback listener on an ephemeral port at lines 791 through 816 and the thread then accepts the first connection that arrives and forwards the whole of standard input to it, with no authentication and no peer validation of any kind, while the tool's own connect at line 848 races that accept. A local process that reaches the port first receives the upload body instead of curl, or disrupts the transfer. Upstream records this as a known security defect at original/docs/KNOWN_BUGS.md:108-121 and names the remedy in the same entry, a random-number check like the one the library's socketpair emulation performs at original/lib/socketpair.c:210-220, while noting that the source address cannot be verified on Windows. | The relay proves the peer before it forwards anything: the tool writes random bytes over the connection it made and the thread forwards standard input only to a peer that presents them, closing any other, which is the check original/docs/KNOWN_BUGS.md:108-121 names and the one original/lib/socketpair.c:210-220 already performs. A legitimate upload is byte for byte what the oracle produces, on standard output, on standard error, in the exit status and in the bytes the server receives. | The oracle hands the whole upload body to whichever local process reaches an ephemeral loopback port first, which is a disclosure of user data the port must not reproduce even bug for bug, the same ground on which DV-0008 refuses to put key material from an unwritten buffer on the wire. Upstream has recorded the defect and prescribed this exact remedy, so the divergence adopts the oracle's own stated intent rather than inventing a policy; the relay is an internal transport for standard input, not part of the command-line contract or the ABI, so nothing a legitimate run can observe changes, and the difference is visible only to a racing local attacker, which no upstream case, corpus or gate exercises. | `curl-cli` (planned) | DL-0240 |

## Entries by owner

| Owner | Entries |
|---|---:|
| `curl-cli` | 1 |
| `curl-dns` | 4 |
| `curl-http2` | 2 |
| `curl-proto-file` | 1 |
| `curl-proto-smb` | 1 |
